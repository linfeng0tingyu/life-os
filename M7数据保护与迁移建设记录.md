---
title: "Life OS M7 数据保护与迁移建设记录"
type: milestone-record
created: 2026-09-22T11:53:25+08:00
updated: 2026-09-22T12:24:00+08:00
status: completed
related:
  - "[[项目说明]]"
  - "[[里程碑计划]]"
  - "[[个人生活管理系统 Life OS v0.1 工程需求提纲]]"
  - "[[接口验收清单]]"
  - "[[部署与迁移指南]]"
  - "[[M6健康日记与财务闭环建设记录]]"
---

# Life OS M7 数据保护与迁移建设记录

## 阶段定位

M7 将此前集中在 `LIFE_OS_HOME` 中的数据库、附件和配置从“便于手工复制”推进为可验证的数据保护闭环。该阶段不改变 schema v4，也不增加外部服务；重点是用 SQLite 自身的一致性机制生成可恢复副本，用开放格式解除数据锁定，并把恢复动作限制在数据库尚未打开的启动阶段。这样既保留了单目录便携模型，也避免在活动连接上直接替换数据库。

## 一致性备份与保留

新增的数据保护服务使用 SQLite backup API 从 WAL 模式主库生成快照。备份先写入同目录临时文件，随后执行 `PRAGMA integrity_check`、读取 `schema_meta.schema_version` 并确认当前程序能够迁移，全部通过后才原子改名。每天第一次正常启动生成 `life-os-YYYYMMDD-auto.db`；同一天再次启动复用已有自动备份。手动备份名称包含微秒，连续操作不会互相覆盖。

`backup.retention_count` 继续使用既有配置字段，允许范围为 1–3650，默认 30；设置页可原子更新该值。成功备份后只清理 `backups/` 根目录中超出数量的受管理自动/手动备份，不处理主数据库、任意用户文件或 `restore-safety/`。备份写入、校验或清理失败会返回明确错误并写入不含业务正文的诊断日志，主数据库不被替换或截断。

## 开放格式导出

全量导出先建立临时数据库快照，再从同一提交时刻读取日历标记、分类、习惯、习惯日志、任务、健康、财务账户和财务流水。CSV 使用稳定显式表头与 UTF-8 BOM；用户文本中以 `= + - @` 起始的值增加电子表格公式保护。财务金额由整数分转换为 CNY 元并固定两位小数，同时保留账户、流水和引用账户的稳定标识。

全部日记按日期写入 `journal/YYYY-MM-DD.md`，本地图片转换为 Base64 数据 URI，使单篇 Markdown 脱离附件目录仍可阅读。`manifest.json` 记录应用版本、schema、时间、编码、金额单位和各文件行数；默认再生成包含整个导出目录的 ZIP。导出使用 `.incomplete` 中间目录，任一步失败即清理中间目录、临时快照和不完整 ZIP，不修改业务数据，也不覆盖历史导出。

## 启动前恢复

设置页恢复操作分为校验与执行两段。用户选择备份并输入“恢复此备份”后，应用只写入 `temp/pending-restore.json` 并提示正常关闭；用户可在关闭前取消。下一次启动先取得 `LIFE_OS_HOME` 实例锁，再处理待恢复请求，完成后才初始化 SQLAlchemy。恢复会再次检查备份，把当前 `life.db` 以及存在的 WAL/SHM 原子移动到 `backups/restore-safety/<timestamp>/`，安装备份并复检；任何安装后错误都会回滚原文件。

如果主数据库已损坏到无法打开设置页，冻结桌面入口和源码诊断入口均支持 `--restore <backup-filename>`。该流程同样要求应用完全关闭、取得实例锁、限制来源为 `backups/` 根目录中的 Life OS 受管理文件，并生成恢复前安全副本。绝对备份路径、父目录跳转和任意外部 SQLite 文件不会被接受。

## 设置界面与接口

新增 `/settings` 页面和侧栏入口，集中显示应用版本、schema、可复制的 `LIFE_OS_HOME`、启动备份状态、最近备份、保留数量、导出结果与恢复反馈。页面只通过同源 API 操作，不把业务数据写入 LocalStorage。新增接口包括：

- `GET /api/system/backups`
- `POST /api/system/backup`
- `POST /api/system/export`
- `PUT /api/system/settings/backup`
- `POST|DELETE /api/system/restore`

`GET /api/system/info` 同步开放当前运行时根目录、数据保护能力、每日启动备份状态和恢复状态。路径只用于本机设置页展示与迁移，不返回环境变量、数据库连接串或其他无关系统路径。

## 验证与演练

专项自动化测试覆盖：多次手动备份与保留清理、每日备份去重、备份完整性、临时 SQLite 辅助文件清理、模拟备份失败后主库字节与查询仍正常、中文 CSV 与 UTF-8 BOM、财务两位小数、Markdown/ZIP/manifest、恢复前安全副本、损坏主库离线恢复、不同绝对路径下复制完整运行时后重新加载，以及全部 M7 API 合同。完整测试套件共 141 项，全部通过。

迁移演练使用两个不同绝对路径：在旧路径写入业务记录并正常 checkpoint，复制完整 `LIFE_OS_HOME` 到新路径，再由新应用实例初始化并读取相同记录。配置文件中不存在旧路径；数据库、备份、导出、恢复状态和 WebView 存储仍由新根目录解析。全新物理 Windows 设备与无 Python 环境的最终发布候选验证继续属于 M8。

真实界面演练已在独立运行时完成设置页检查、手动备份、全量 ZIP 导出、恢复请求创建与取消，并再次创建请求后重启程序。重启结果确认当前数据库先进入 `backups/restore-safety/`，指定备份恢复成功，待处理请求清空，浏览器控制台无错误。PyInstaller `onedir` 发行包已重新构建，冻结后的 `LifeOS.exe --check` 能在全新绝对路径初始化 schema v4、生成每日备份，且备份目录无临时 WAL/SHM 残留。

## 阶段边界

M7 不提供云同步、自动异地上传、加密密钥管理、CSV 导入或跨版本双向同步。应用内备份保护 SQLite 一致性，但不能替代独立介质和设备级加密；用户仍应在 Life OS 关闭后复制完整运行时目录或最新导出 ZIP。M8 将在干净 Windows 10/11 环境完成最终发行、迁移、恢复与异常场景验收。
