---
title: "个人生活管理系统 Life OS"
type: project
created: 2026-09-18T14:17:59+08:00
updated: 2026-09-18T15:04:44+08:00
status: active
related:
  - "[[个人生活管理系统 Life OS v0.1 工程需求提纲]]"
  - "[[里程碑计划]]"
  - "[[数据库模型]]"
  - "[[接口验收清单]]"
  - "[[部署与迁移指南]]"
---

# 个人生活管理系统 Life OS

## 项目目标

开发一个完全运行于个人电脑本地的个人生活管理系统，以日期为中心统一管理任务、习惯、睡眠、体重、身心状态和日记。v0.1 应形成可持续演进的最小产品：数据可靠保存，所有应用数据与缓存可随一个运行时根目录迁移，并能通过开放格式导出。

项目达到 v0.1 完成状态时，应满足 [[个人生活管理系统 Life OS v0.1 工程需求提纲#十五、v0.1 验收标准|v0.1 验收标准]]，通过全部核心自动化测试，并完成一次跨目录迁移和数据库恢复演练。

## 背景

该项目不是单纯的 Todo List 或习惯打卡工具，而是个人生活数据的长期基础设施。第一阶段选择 Python、Flask、SQLAlchemy、SQLite 与原生 Web 前端，以降低部署和维护成本。数据库、配置、备份、导出、日志、临时文件及应用缓存统一保存在 `LIFE_OS_HOME`，避免运行状态散落在设备各处。

## 范围边界

v0.1 包含月历与 Today Dashboard、工作日/休息日及节假日/自定义日期标记、任务、动态习惯、健康与睡眠、体重、Markdown 日记、自动保存、备份、导出、Windows 启动脚本和核心测试。

v0.1 不包含 AI 分析、复杂统计、云同步、账户体系、局域网访问、手机 App、财务、通知、重复任务、子任务界面、日记标签、复杂富文本和完整数据导入。新增范围必须先评估是否影响数据库兼容性与里程碑顺序。

## 关键约束与决定

- 默认仅监听 `127.0.0.1`，不提供远程访问。
- SQLite 是权威运行数据库，CSV 与 Markdown 是开放交换格式。
- 应用生成的可变状态只能写入 `LIFE_OS_HOME`；代码中不得散落数据路径。
- 重要业务数据不得依赖 LocalStorage、浏览器缓存或 Python 安装缓存。
- 数据库写操作必须具备事务、校验和错误反馈；删除默认采用归档或软删除。
- 先完成数据层和可靠性，再完善界面；不引入 Docker、Node.js 构建链或独立数据库服务。

## 项目文档

- [[个人生活管理系统 Life OS v0.1 工程需求提纲]]：产品范围、技术基线和最终验收条件。
- [[里程碑计划]]：开发顺序、交付物、阶段出口和依赖关系。
- [[数据库模型]]：v0.1 表结构、约束、索引和演进规则。
- [[接口验收清单]]：通用 API 合同及逐接口验收项目。

## 当前状态

项目处于 `active`，M0–M2 已完成。工程现已具备 Flask 与可移植运行时基础，以及自动初始化的 SQLite schema、版本检查、完整性检查、WAL/外键配置和日期分类、习惯、任务、健康、睡眠、日记领域服务。日期模型支持工作日、休息日、节假日名称和自定义标签，显式记录覆盖星期推断。被移植设备的环境、配置和验证流程记录在 [[部署与迁移指南]]。

M2 完成后的 38 项自动化测试已通过，覆盖建库、schema 版本、无版本数据库阻断、SQLite pragma、重启持久性、数据库约束、事务回滚和全部领域服务。独立临时运行时的 `app.py --check` 已成功创建数据库。当前无技术阻塞；业务 HTTP API 尚未实现。

## 下一步计划

等待用户说明下一步。在获得明确指示后，从 [[里程碑计划#M3：核心 API|M3：核心 API]] 开始，不提前实现正式前端业务交互。

## 项目复盘

项目结束或中止时补充结果、范围偏差、迁移与恢复演练结论、可复用工程经验，以及需要回流 Research 或 Knowledge 的问题。
