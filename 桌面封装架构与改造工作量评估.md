---
title: "Life OS 桌面封装架构与改造工作量评估"
type: architecture-assessment
created: 2026-09-20T10:17:02+08:00
updated: 2026-09-20T10:17:02+08:00
status: draft
related:
  - "[[项目说明]]"
  - "[[个人生活管理系统 Life OS v0.1 工程需求提纲]]"
  - "[[前端设计与技术架构提纲]]"
  - "[[部署与迁移指南]]"
  - "[[里程碑计划]]"
---

# Life OS 桌面封装架构与改造工作量评估

## 决策与当前状态

Life OS 正式发行架构确定为 **Python + Flask + Waitress + pywebview + PyInstaller onedir**。现有 HTML、CSS、Vanilla JavaScript、REST API、领域服务、SQLAlchemy 和 SQLite 全部保留，不改写为 C#，也不引入 Node.js、Electron、Tauri 或新的前端构建链。

当前版本仍通过 `start.bat` 启动 Flask 开发服务器并打开外部浏览器；桌面封装尚未实施。因此，在完成本架构改造前，`start.bat` 仍是现有可用入口，但不再被定义为最终发行形态。完成改造后，普通用户双击 `LifeOS.exe` 即可打开独立窗口，不需要安装 Python、不显示终端，也不需要手工访问 `127.0.0.1`。`start.bat` 和 `app.py` 保留为开发、自动化测试和故障诊断入口。

## 目标运行链路

```text
LifeOS.exe（PyInstaller onedir，windowed）
  → Desktop Entry / 生命周期控制器
    → 初始化 LIFE_OS_HOME、配置、日志、实例锁和数据库
    → 预绑定 127.0.0.1 随机可用端口
    → Waitress WSGI 服务
      → Flask 页面与 /api
        → 领域服务
          → SQLAlchemy / SQLite
    → pywebview 原生窗口
      → Windows WebView2
        → 加载同源本地页面
```

桌面窗口只是现有 Web 前端的宿主，不获得直接访问数据库或文件系统的权限。页面仍通过同源 `fetch` 调用 Flask API，业务规则继续只存在于领域服务中。桌面宿主只负责启动、窗口、端口、关闭和打包资源定位，不承担业务逻辑。

## 组件边界

| 组件 | 职责 | 不承担的职责 |
|---|---|---|
| `desktop_entry.py` | 解析 `--home`、初始化运行时、启动服务、创建窗口、协调关闭 | 不实现领域业务或直接写数据库 |
| Desktop Host | 管理 pywebview 主线程、窗口尺寸、WebView 缓存目录和启动错误页 | 不向 JavaScript 暴露任意 Python 对象 |
| WSGI Host | 使用 Waitress 仅监听 `127.0.0.1`，报告就绪并支持受控关闭 | 不监听局域网地址，不使用 Flask 开发服务器 |
| Flask App | 继续提供 Jinja 页面、静态资源、API、错误合同和请求日志 | 不感知窗口布局或调用桌面 GUI |
| PyInstaller Spec | 收集 Python 依赖、模板、静态资源、图标和版本信息 | 不包含用户数据库、日志、缓存或测试数据 |
| `LIFE_OS_HOME` | 保存所有可变业务数据和应用/WebView 缓存 | 不保存程序二进制和 Python 运行时 |

pywebview 的 JavaScript–Python bridge 默认不启用。现有 REST API 已能完成业务读写，没有必要额外开放 `window.pywebview.api`，从而避免形成第二套绕过 API 和领域服务的数据入口。

## 启动与关闭生命周期

1. `LifeOS.exe` 解析 `--home`，按现有优先级定位 `LIFE_OS_HOME`。
2. 初始化目录、日志、设置、实例锁、数据库与 schema，并在任何窗口创建前完成失败检查。
3. 创建仅绑定 `127.0.0.1` 的预绑定 socket，由操作系统分配可用端口，再交给 Waitress，避免固定端口冲突和“检查后被抢占”的竞态。
4. WSGI 服务在后台线程运行；桌面主线程等待健康检查成功后创建 pywebview 窗口。
5. pywebview 加载本次进程的内部 URL。该 URL 不作为用户操作入口，也不写入长期配置。
6. 用户关闭窗口时，先停止接收新请求，等待短时在途写请求结束，再关闭 WSGI 服务、执行 SQLite WAL checkpoint、释放实例锁并退出。
7. 若窗口未创建前发生错误，使用本地错误页面或最小窗口显示可理解的错误与日志位置，不依赖终端输出。

必须通过测试证明正常关闭后没有残留 `LifeOS.exe`、Python 子进程、监听端口或数据库锁。强制结束进程仍可能跳过应用级 checkpoint，但 SQLite 事务与 WAL 不得因此产生半写入业务记录。

## 便携目录与数据边界

目标发布包采用 PyInstaller `onedir`，不先使用 `onefile`。`onedir` 不需要每次启动把程序解压到系统临时目录，资源定位和故障诊断更稳定，也更符合当前“复制整个软件目录即可迁移”的目标。

```text
Life OS/
├── LifeOS.exe
├── _internal/                 # Python、依赖与程序资源
├── life-os-data/              # 默认 LIFE_OS_HOME
│   ├── config/
│   ├── database/
│   ├── backups/
│   ├── exports/
│   ├── cache/
│   │   └── webview/           # WebView 用户数据与可重建缓存
│   ├── logs/
│   ├── temp/
│   └── metadata.json
└── README.md
```

pywebview 的 `storage_path` 必须显式设置为 `LIFE_OS_HOME/cache/webview/`。程序不得采用 pywebview 的默认用户目录，也不得让 WebView2 用户数据散落到 `%APPDATA%`。业务数据仍只存在于 SQLite 和经批准的导出文件中；WebView 缓存是可删除、可重建内容，不能成为业务数据唯一副本。

打包模式下，程序资源根目录从 `sys.executable` 和 PyInstaller 资源位置解析，数据根目录始终独立解析。禁止把 `life.db`、设置或日志写入 `_MEIPASS`、`_internal` 或只读程序资源目录。

## 依赖与构建分层

正式运行依赖增加：

- `waitress`：替代 Flask 开发服务器，在 Windows 上承载本地 WSGI 服务。
- `pywebview`：创建桌面窗口；Windows 优先使用 Edge Chromium/WebView2。

构建依赖独立放入 `requirements-build.txt`：

- `pyinstaller`：生成 Windows `onedir`、无控制台窗口的发布目录。

开发依赖继续放在 `requirements-dev.txt`。目标设备不需要 Python、pip、虚拟环境或外部浏览器，但必须具备 Microsoft Edge WebView2 Runtime；构建与发布检查应检测该依赖，并在缺失时给出明确说明。首次启动和日常使用都不得联网下载 Python 包或前端资源。

## 打包与发布策略

- 使用版本受控的 `LifeOS.spec`，显式收集 `life_os/templates/`、`life_os/static/` 和必要元数据。
- 初始交付只生成 Windows x64 `onedir`，不同时引入安装器、自动更新或单文件发布。
- `--windowed` 隐藏控制台；日志继续写入 `LIFE_OS_HOME/logs/`。
- 生成物必须从全新 `dist/` 构建，不能把开发 `.venv`、测试缓存、默认生活数据或本机绝对路径带入发布包。
- 构建完成后在没有 Python 的干净 Windows 用户环境执行首次启动、关闭、重启、迁移和离线验证。
- 浏览器模式作为诊断路径继续受测试，但不作为普通用户的默认体验。

## 工作量评估

以下为一名熟悉现有工程的开发者完成“可发布质量”改造的净工程量，不包含等待用户验收、代码签名证书采购或 Windows 安装器开发。

| 工作包 | 主要内容 | 预计人日 | 风险 |
|---|---|---:|---|
| A. 依赖与入口拆分 | 增加 Waitress、pywebview、构建依赖；区分浏览器入口与桌面入口 | 0.5–1.0 | 低 |
| B. 桌面生命周期 | 后台 WSGI、就绪探测、窗口创建、关闭协调、异常启动页 | 1.5–2.0 | 中 |
| C. 端口与单实例 | 预绑定随机回环端口、现有文件锁复用、残留进程/端口验证 | 1.0–1.5 | 中 |
| D. 冻结路径与缓存 | 适配 PyInstaller 资源路径；WebView 存储进入 `LIFE_OS_HOME/cache/webview` | 1.0–1.5 | 高 |
| E. PyInstaller 构建 | `LifeOS.spec`、资源收集、无控制台构建、版本与图标、构建脚本 | 1.0–1.5 | 中 |
| F. 自动化测试 | 生命周期单测、资源定位、无外部写入、关闭与锁释放、冻结态冒烟测试 | 1.5–2.0 | 高 |
| G. 干净机与迁移验收 | 无 Python 环境、WebView2 检测、离线启动、跨路径/跨设备迁移 | 1.0–1.5 | 中 |
| H. 文档与发布清单 | README、部署迁移、故障处理、依赖许可证和发布核查 | 0.5–1.0 | 低 |
| **合计** | **正式可发布的便携桌面版** | **8–12 人日** | **中** |

可在约 2–3 人日内得到能够打开窗口的技术原型，但原型不应作为正式发布版：它通常尚未覆盖优雅关闭、冻结资源定位、无 Python 机器、WebView 缓存边界和迁移回归。若以后增加安装器、代码签名、开始菜单快捷方式、自动更新或 Windows 原生通知，预计另增 2–4 人日，并可能产生证书或分发基础设施成本。

## 建议实施顺序

建议把本改造作为 [[里程碑计划#M4.5：桌面封装与便携发行基础|M4.5]]，在 M5 大量增加交互前完成：

1. 先建立可测试的 WSGI Host 与桌面入口，不立即打包。
2. 用现有全部 API 和 M4 页面完成 pywebview 窗口烟雾测试。
3. 固化 `LIFE_OS_HOME`、WebView 缓存、端口和关闭规则。
4. 建立 PyInstaller `onedir` 构建并解决资源定位。
5. 补齐冻结态测试、无 Python 环境和迁移验收。
6. 桌面版达到出口条件后再继续 M5；后续 M5–M7 功能无需重新处理桌面宿主。

## M4.5 验收清单

- 双击 `LifeOS.exe` 只出现一个 Life OS 窗口，不出现终端和外部浏览器。
- 目标设备没有 Python、pip 和 `.venv` 时可以离线启动。
- 服务只监听 `127.0.0.1`，不监听局域网地址；内部端口冲突可自动避免。
- 关闭最后一个 Life OS 窗口后，服务、线程、端口和实例锁全部释放。
- 正常关闭执行数据库 checkpoint；强制结束后重新启动仍能通过数据库完整性检查。
- 模板、CSS、JavaScript、SVG 和版本化资源在冻结模式下全部可读取。
- 数据库、配置、日志、临时文件、应用缓存和 WebView 缓存全部位于同一个 `LIFE_OS_HOME`。
- 发布目录不包含开发数据库、个人日志、测试缓存、`.venv` 或构建机绝对路径。
- 自定义 `--home`、环境变量和默认相邻 `life-os-data/` 三种路径均通过测试。
- `start.bat` 与 `app.py` 继续支持开发和诊断，不与桌面实例争用同一运行时目录。
- 在无 Python 的干净 Windows 10/11 x64 环境完成启动、写入、关闭、重启和迁移演练。

## 主要风险与控制

| 风险 | 影响 | 控制措施 |
|---|---|---|
| WebView2 Runtime 缺失或版本异常 | 窗口无法创建 | 启动前检测并提供明确说明；发布说明列为目标环境要求 |
| PyInstaller 遗漏模板或静态文件 | 页面 404 或样式缺失 | spec 显式收集资源；冻结态页面与静态资源测试 |
| 默认 pywebview 存储写入用户目录 | 破坏单目录迁移原则 | 强制 `storage_path=LIFE_OS_HOME/cache/webview` 并做外部写入审计 |
| 关闭窗口时仍有写请求 | 数据保存状态不明确 | 关闭协调、短时等待、服务停止顺序与 checkpoint 测试 |
| Waitress 与 GUI 线程生命周期互锁 | 退出挂起或残留进程 | GUI 只在主线程；WSGI 独立宿主；超时和线程存活测试 |
| `onefile` 临时解压和路径差异 | 启动慢、资源与缓存边界复杂 | v0.1 只支持 `onedir` |
| 打包后安全软件误报 | 用户无法启动 | 保持依赖精简、发布哈希；代码签名作为后续可选工作 |

## 参考依据

- [Flask：Deploying to Production](https://flask.palletsprojects.com/en/stable/deploying/)：正式运行不应使用内置开发服务器。
- [Flask：Waitress](https://flask.palletsprojects.com/en/stable/deploying/waitress/)：Waitress 直接支持 Windows。
- [Waitress：Socket Activation](https://docs.pylonsproject.org/projects/waitress/en/latest/socket-activation.html)：支持接收预绑定 socket。
- [pywebview：Introduction](https://pywebview.idepy.com/en/guide/) 与 [API](https://pywebview.idepy.com/en/guide/api)：桌面窗口、WebView 引擎和 `storage_path` 边界。
- [pywebview：Freezing](https://pywebview.idepy.com/en/guide/freezing)：Windows/Linux 可使用 PyInstaller 冻结。
- [PyInstaller：Operating Mode](https://pyinstaller.org/en/stable/operating-mode.html)：`onedir`、`onefile` 与无控制台窗口的打包差异。

来源核验日期：2026-09-20。
