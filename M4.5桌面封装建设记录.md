---
title: "Life OS M4.5 桌面封装建设记录"
type: project-milestone-record
created: 2026-09-20T14:51:41+08:00
updated: 2026-09-20T21:49:18+08:00
status: completed
related:
  - "[[项目说明]]"
  - "[[里程碑计划]]"
  - "[[桌面封装架构与改造工作量评估]]"
  - "[[部署与迁移指南]]"
  - "[[前端设计与技术架构提纲]]"
  - "[[界面主题设计规范]]"
---

# Life OS M4.5 桌面封装建设记录

## 建设结论

M4.5 已建立 Windows 独立桌面窗口和便携发行基础。原 M1–M4 的 Flask app factory、运行时目录、实例锁、SQLite/SQLAlchemy、领域服务、REST API、Jinja 页面、Vanilla JavaScript 和响应式样式均被直接复用，没有引入 C#、Node.js、Electron 或第二套业务接口。普通用户可从 PyInstaller `onedir` 目录双击 `LifeOS.exe`；源码浏览器模式继续作为开发和诊断入口。

当前桌面运行链为：

```text
LifeOS.exe
  → 解析并初始化 LIFE_OS_HOME
  → 获取实例锁并检查 SQLite
  → 预绑定随机 127.0.0.1 端口
  → Waitress 后台服务与健康检查
  → pywebview / WebView2 独立窗口
  → 既有 Flask 页面与 REST API
```

## 对 M1–M4 的适配

| 原阶段 | 本次适配 | 保持不变的边界 |
|---|---|---|
| M1 | 冻结模式默认数据目录改为 `LifeOS.exe` 同级 `life-os-data/`；新增 `cache/webview/`；源码依赖检查加入 Waitress 与 pywebview | 配置、日志、实例锁、`LIFE_OS_HOME` 优先级和源码入口 |
| M2 | 窗口退出路径接入 SQLite WAL checkpoint，并确保服务停止异常时仍释放数据库与实例锁 | schema v2、模型、事务和领域规则 |
| M3 | 桌面 Host 使用 `/api/system/health` 判断服务就绪，页面继续通过同源 REST API 读写 | JSON 合同、日期聚合和领域接口 |
| M4 | 现有模板、CSS、JavaScript、SVG、七套主题和统一 Logo 被打包并在 WebView2 中直接运行；窗口根据屏幕尺寸居中和缩放 | Today/Calendar 信息架构、响应式断点和业务逻辑 |

## 实现清单

- `desktop_entry.py` 提供 `--home`、`--check`、冻结态错误提示和桌面启动入口。
- `life_os/desktop/server.py` 使用 Waitress 接收预绑定 socket，只监听 `127.0.0.1` 的随机端口，并提供健康探测和受控停止。
- `life_os/desktop/host.py` 管理窗口、服务、checkpoint 和实例锁生命周期；Windows 固定使用 Edge Chromium/WebView2，不启用 `js_api`、文件 URL、下载、开发者工具或远程调试端口。
- pywebview `storage_path` 固定为 `LIFE_OS_HOME/cache/webview/`，WebView 缓存、主题偏好和持久化界面状态不再散落到用户目录。
- `LifeOS.spec` 显式收集应用模板、静态资源、pywebview 运行资源、图标和 Windows 版本信息，并排除不使用的 Qt、GTK、CEF、Android 与 Cocoa 后端。
- `build_desktop.bat` 安装构建依赖，从指定 Logo 源文件生成多尺寸 `.ico`，清理并重建 `dist/LifeOS/`，随后复制 README 和部署迁移指南。
- `requirements.txt` 新增 Waitress 与 pywebview；`requirements-build.txt` 独立保存 PyInstaller 与 Pillow 构建依赖。
- `build/`、`dist/` 和生成图标目录被 Git 忽略，发行包不进入源码提交，也不会携带用户数据。

## 验证记录

| 验证项 | 结果 |
|---|---|
| 自动化测试 | 主题增量后 101 项 pytest 全部通过 |
| Waitress | 随机回环端口可访问健康接口；停止后端口不可连接 |
| 生命周期 | 正常关闭后服务、进程和实例锁释放；服务停止异常时仍执行 checkpoint 与锁释放 |
| 冻结路径 | 默认 `life-os-data/` 位于 `LifeOS.exe` 同级，不写入 `_internal/` |
| 单目录状态 | 数据库和 WebView 存储均在指定 `LIFE_OS_HOME`；中文绝对路径通过检查 |
| 无开发环境检查 | 清空 Python 相关 PATH 后，冻结包 `--check` 返回 0 并创建预期数据库与缓存目录 |
| 发行内容 | `README.md` 与 `部署与迁移指南.md` 已随包复制；发行目录不含 `life-os-data/` |
| Windows 元数据 | 产品名 `Life OS`，文件版本 `0.1.0.0`，自定义应用图标已写入 EXE |
| 独立窗口 | 仅出现一个标题为 `Life OS` 的窗口，不出现终端或外部浏览器；M4 Today 页面与本地 SVG 图标正常显示 |
| 主题与品牌 | 七套主题逐项切换并完成窗口视觉检查；页面 Logo 与源 PNG 哈希一致；随机端口重启后恢复吉卜力风格 |
| 退出 | 通过窗口关闭后 `LifeOS.exe` 退出，无残留主进程 |

本轮构建环境为 Windows 11、Python 3.14.6、Waitress 3.0.2、pywebview 6.2.1、pythonnet 3.1.0、PyInstaller 6.22.3 和 Pillow 12.3.0。未运行的基础 `dist/LifeOS/` 约 38.0 MiB；首次运行产生的 `life-os-data/cache/webview/` 属于可删除、可重建的运行数据，不计入基础程序体积。

## 已知边界与后续

M4.5 交付的是可运行、可重复构建的桌面便携基础，不等同于 v0.1 正式发布候选。当前没有安装器、代码签名、自动更新或开始菜单快捷方式；Windows 设备必须具备兼容的 Microsoft Edge WebView2 Runtime。缺少 WebView2 或启动失败时，冻结入口会使用本地消息框报告错误，详细运行信息仍以 `LIFE_OS_HOME/logs/app.log` 为准。

本轮通过限制 PATH 验证发行包不依赖外部 Python，但没有卸载构建机上的 Python，也没有替代 M8 的全新物理 Windows 10/11 设备验证。跨设备迁移、完整业务写入后重启、备份恢复、安全软件兼容和最终发布清单应在 M7–M8 完成。

## 2026-09-20 主题与 Logo 增量

PyInstaller 的 `static/` 收集规则自动纳入 `themes.css`、`theme.js` 和 `images/life-os-logo.png`，无需新增资源旁路。桌面主题选择通过 WebView LocalStorage 保存当前端口状态，并以同一配置目录内的本地 Cookie 解决随机端口重启后的恢复问题；二者均位于既有 `cache/webview/`，不写入 SQLite 或 `settings.json`，也不增加主题 API。图标生成脚本不再绘制旧占位图形，而是读取 `design-assets/logo-concepts/life-os-logo-02a-ref-a-full-landscape-contained.png`、裁切透明边界并生成 Windows 多尺寸 ICO，使页面品牌与 EXE 图标保持同源。
