---
created: 2026-09-18T13:51:18+08:00
updated: 2026-09-22T11:53:25+08:00
status: draft
---

# 个人生活管理系统 Life OS v0.1

## 一、项目定位

Life OS 是一个完全运行于个人电脑本地的个人生活管理系统。它以日期为主要组织维度，将任务、习惯、睡眠、体重、身心状态、日记与个人财务保存在统一的个人生活数据库中，为后续统计、周期复盘、AI 分析和其他个人系统接入提供稳定的数据基础。

v0.1 的目标是建立一个可靠、易迁移、可长期维护的最小可用系统，而不是一次性实现完整的 Personal OS。数据结构、数据安全和可恢复性优先于视觉效果与功能数量。

## 二、产品与工程原则

1. 系统默认仅在本机运行和访问，不依赖云服务器、在线账户或第三方同步服务。
2. 核心数据使用 SQLite 保存；不得将重要生活数据仅保存在浏览器 LocalStorage、缓存或内存中。
3. 所有应用生成的可变数据与缓存集中在一个可迁移的运行时目录中，复制该目录即可完成数据迁移与恢复。
4. 数据应能导出为 CSV 和 Markdown 等开放格式，避免被应用或框架锁定。
5. 技术方案保持轻量：Python、Flask、SQLAlchemy、HTML、CSS 和 Vanilla JavaScript；正式桌面发行使用 Waitress、pywebview 和 PyInstaller，不引入 C#、Electron 或 Node.js 构建系统，也不依赖 Docker 或独立数据库服务。
6. UI、API、业务服务、数据库模型和数据访问逻辑应分离，但避免微服务、过度抽象和无必要的依赖。
7. v0.1 优先支持 Windows 10/11 桌面环境，同时避免采用阻碍未来跨平台或响应式布局的设计。
8. 界面主题属于可替换的表现层：切换风格、配色和部分图标表现不得改变业务字段、API、领域服务或数据库；主题偏好可以丢失并回退默认，但业务数据不能依赖主题缓存。

## 三、技术方案

### 3.1 后端

- Python 3.12+
- Flask
- Waitress，作为正式桌面发行包内的本地 WSGI 服务；Flask 开发服务器只用于源码调试
- SQLAlchemy；可使用 Flask-SQLAlchemy，但不同时维护两套数据库访问方式
- SQLite
- REST 风格 JSON API

数据库初始化应随首次启动自动完成，不要求用户手工执行 SQL。v0.1 可使用明确的初始化逻辑；数据库结构开始演进后应引入 Alembic 等迁移机制，并保存 schema 版本。

### 3.2 前端

- HTML5、CSS3、Vanilla JavaScript
- 使用 `fetch` 调用后端 API，不因保存操作刷新整个页面
- 正式运行时由 pywebview 提供独立桌面窗口，Windows 使用 WebView2 渲染；外部浏览器仅作为开发与故障诊断入口
- pywebview 只承担窗口与生命周期管理，业务读写仍统一通过 REST API，不启用新的 JS-Python 业务桥接
- 支持“默认、古风竹青、古风藏青、古风水色、新拟物派、macOS 毛玻璃、吉卜力风格”七套本地主题；主题选择由 CSS Token 与组件覆盖实现，不复制页面和业务脚本
- 项目 Logo 固定使用 `design-assets/logo-concepts/life-os-logo-02a-ref-a-full-landscape-contained.png`，页面使用其本地静态副本，桌面图标从同一源文件生成
- 页面品牌 Logo 与 Windows 应用图标应充分利用图标画布，视觉尺寸参考 Chrome、ChatGPT 等常用桌面应用，不能因透明留白而显得明显偏小
- v0.1 不引入 React、Vue、Angular 或 Node.js 构建链
- Chart.js、FullCalendar、Markdown 渲染器等库只有在确有需要时才引入，并应固定版本、随应用本地分发，不以 CDN 可用为运行前提

## 四、统一运行时目录与可移植性

### 4.1 目录规则

系统只允许在一个运行时根目录 `LIFE_OS_HOME` 中写入应用生成的可变内容。默认位置为项目目录下的 `life-os-data/`，也允许通过环境变量或启动配置指定其他绝对路径。源代码目录除随程序提供的静态资源外，不得散落数据库、日志、导出文件或缓存。

建议结构如下：

```text
life-os-data/
├── config/
│   └── settings.json
├── database/
│   └── life.db
├── backups/
├── attachments/
│   └── journal/
├── exports/
│   └── journal/
├── cache/
│   └── webview/
├── logs/
│   └── app.log
├── temp/
└── metadata.json
```

以下内容必须位于 `LIFE_OS_HOME`：

- SQLite 数据库及其 WAL、SHM 等伴随文件
- 用户配置和运行时状态
- 自动与手动备份
- 日记图片等用户附件
- CSV、Markdown、ZIP 等导出产物
- 应用自行生成的缓存、索引和缩略数据
- 应用日志与临时文件

Python 虚拟环境、pip 下载缓存和开发者外部浏览器的缓存不属于 Life OS 的应用数据；系统不得依赖这些外部缓存保存状态。正式桌面窗口使用的 pywebview/WebView2 缓存、LocalStorage、Cookie 与其他持久化存储必须显式位于 `LIFE_OS_HOME/cache/webview/`，只可保存主题选择等可丢弃的界面偏好，不得保存唯一副本的业务数据。主题偏好键固定为 `life-os.theme`：当前端口使用 LocalStorage，跨随机端口重启使用同一 WebView 配置中的本地 Cookie 回退；无效或缺失时回退“默认”。

### 4.2 路径与迁移约束

- 所有子路径都由 `LIFE_OS_HOME` 派生，集中在一个配置模块中解析，不得在业务代码中重复硬编码。
- 数据库和配置中不得保存指向旧设备的非必要绝对路径；需要引用目录内文件时使用相对于 `LIFE_OS_HOME` 的路径。
- 程序启动时检查目录是否存在、是否可写以及数据库版本是否兼容；失败时给出明确错误，不得静默创建到其他位置。
- 迁移的标准流程为：正常关闭程序，复制整个 `LIFE_OS_HOME`，在新设备上指定该目录并启动。README 必须说明该流程。
- 系统应防止两个进程同时写入同一运行时目录，至少通过端口检测、进程锁或等效机制给出明确提示。
- 迁移前建议创建一次一致性备份；不得在数据库仍有未提交事务时直接复制主数据库文件。

## 五、应用结构

建议代码结构如下；v0.1 可按规模合并文件，但模型、路由、服务和前端逻辑应保持边界：

```text
life-os/
├── app.py                       # 源码开发与诊断入口
├── desktop_entry.py             # 正式桌面入口
├── requirements.txt
├── requirements-dev.txt
├── requirements-build.txt
├── LifeOS.spec
├── README.md
├── start.bat
├── build_desktop.bat
├── life_os/
│   ├── __init__.py              # Flask app factory
│   ├── runtime.py
│   ├── settings.py
│   ├── desktop/                 # M4.5：窗口与 WSGI 生命周期
│   ├── models/
│   ├── routes/
│   ├── services/
│   │   ├── backup_service.py
│   │   ├── export_service.py
│   │   └── statistics_service.py
│   ├── templates/
│   ├── static/
│   │   ├── css/
│   │   │   └── themes.css       # 七套主题 Token 与组件装饰
│   │   ├── js/
│   │   │   └── theme.js         # 主题校验、切换与本地偏好
│   │   ├── images/              # Logo 等本地品牌资源
│   │   └── vendor/
│   └── utils/
└── tests/
```

应用调用链为：pywebview 桌面窗口（正式运行）或外部浏览器（开发诊断）→ REST API → 业务服务 → SQLAlchemy → SQLite。桌面宿主不得绕过 API 直接修改业务数据；备份、导出和未来的 AI 功能通过服务层访问数据，不直接绕过业务规则修改数据库。

## 六、日期中心模型

系统采用 Date-Centric 设计。用户选择某个日期后，应在同一页面查看或编辑该日的工作/休息属性、节假日与自定义标记、任务、习惯、睡眠、体重、身心状态、日记和当日财务流水。生活日期统一使用本地时区的 `YYYY-MM-DD`；具体事件时间使用包含日期与时间的 ISO 8601 字符串。v0.1 不引入复杂的跨时区换算。

### 6.1 日期属性与标记

日历需要区分 `workday`（工作日）和 `rest_day`（休息日）。日期解析按“用户显式记录 → 已正式公布的国务院节假日预置 → 星期默认值”的顺序执行；用户设置始终优先，以支持个人休息安排和值班覆盖。未公布年份不得按经验猜测调休日期，只按周一至周五为工作日、周六和周日为休息日推断。

每个日期最多保存一条显式记录，至少包括：`date`、`day_type`、`holiday_name`、`custom_label`、`note`、`source`、`archived_at`、`created_at` 和 `updated_at`。

- 用户可以把任意日期明确标记为工作日或休息日。
- `holiday_name` 用于“国庆节”等节假日名称；节假日通常同时标记为休息日。
- `custom_label` 允许记录“年假”“调休上班”“纪念日”等用户自定义短标签，不改变 `day_type` 的明确含义。
- 清除显式标记后恢复法定节假日预置；该日没有官方预置时再恢复星期推断，历史记录采用归档方式保留。
- 程序应随版本内置已经由国务院办公厅正式公布的中国大陆法定节假日和调休工作日，记录通知年份与文号；更新官方日历不写入或改写用户数据库。
- 月历单元在公历日期下显示农历日期；农历由本地运行时离线派生，不依赖网络接口，不作为可编辑业务数据写入 SQLite。

### 6.2 习惯

习惯定义必须动态存储，不得写死在代码中。

`habits` 至少包含：`id`、`name`、`description`、`icon`、`category`、`active`、`sort_order`、`created_at`、`updated_at`。

`habit_logs` 至少包含：`id`、`habit_id`、`date`、`status`、`value`、`note`、`created_at`、`updated_at`，并对 `(habit_id, date)` 设置唯一约束。v0.1 的主要交互是完成/未完成；`value` 为分钟、距离、次数等量化记录预留。

用户应能新增、编辑、排序、停用和重新启用习惯。习惯分类采用可创建的单选选项：分类按任务/习惯作用域分别管理，用户可以在编辑时直接创建并选中新分类，不允许自由文本产生未登记分类。点击当天习惯状态后立即保存，刷新或重启后状态保持不变。

### 6.3 任务

`tasks` 至少包含：`id`、`title`、`description`、`status`、`priority`、`category`、`created_at`、`updated_at`、`due_date`、`scheduled_date`、`completed_at`、`sort_order`、`parent_id`、`archived_at`。

- 状态支持 `todo`、`doing`、`done`、`cancelled`；v0.1 界面可以只突出未完成和完成。
- 优先级支持 `low`、`normal`、`high`、`urgent`。
- 支持有日期任务和无日期的长期任务；一天可以有多个任务。
- `parent_id` 为子任务预留，v0.1 不实现子任务界面。
- 完成、取消和归档任务保留历史记录；用户明确执行永久删除前不物理删除。
- 任务分类采用与习惯一致的可创建单选交互，但任务与习惯分类列表互不混用。

当天页面应能区分计划于当日、当日到期和此前未完成的任务，并支持新增、编辑、完成、取消、归档、调整优先级和修改日期。

### 6.4 健康与睡眠

`daily_health` 每个日期最多一条记录，至少包含：`id`、`date`、`weight`、`sleep_start`、`sleep_end`、`sleep_duration`、`sleep_quality`、`energy_level`、`mood_level`、`body_status`、`exercise_minutes`、`note`、`created_at`、`updated_at`。

- `date` 必须唯一。
- 睡眠时长由入睡和起床时间自动计算，并允许用户修正。
- 睡眠质量、精力和情绪统一使用 1–5 评分；界面应解释各分值含义。
- 体重等数值字段需校验合理格式，但不因缺少其他健康字段而阻止保存。
- Today 与日历日期详情中的生活节律概览只展示睡眠时长、体重、运动和身体健康；睡眠起止、睡眠质量、精力、情绪及健康备注等详细字段保留在数据层，并在独立生活节律板块中展开。

### 6.5 日记

`journals` 至少包含：`id`、`date`、`content`、`created_at`、`updated_at`，其中 `date` 唯一。v0.1 采用一天一篇，内容支持 Markdown，并提供一级至三级标题、本地图片插入、安全预览和单日导出；工具栏可把设备当前 24 小时制的时与分插入光标位置。图片文件必须保存在 `LIFE_OS_HOME/attachments/journal/`，正文只保存可迁移的受控相对 URL；单日导出把图片内嵌为自包含 Markdown。标签、一天多篇和可执行 HTML 型富文本暂不实现。日记在停止输入约 1–2 秒后自动保存，并明确显示“保存中”“已保存”或“保存失败”。

### 6.6 个人财务

v0.1 提供当日记账与个人总体财务摘要。`finance_accounts` 保存现金、银行、信用、投资或其他账户，区分资产和负债；`finance_transactions` 保存收入、支出和账户间转账。

- 金额以整数分存储，API 使用两位小数字符串；v0.1 统一使用 CNY，不做汇率换算。
- 收入增加目标账户余额，支出减少来源账户余额，转账不计入收入或支出。
- 当日页面应展示当日流水、收入、支出和净现金流，并支持新增、编辑和归档流水。
- 记账分类采用 `finance` 作用域的单选项，并允许在记账窗口中现场新增；任务、习惯和财务分类互不混用。
- 总体财务摘要至少包含各账户当前余额、总资产、总负债、净资产，以及指定日期范围的收入、支出和净现金流。
- 停用账户保留历史流水并继续列在账户管理中，以便重新启用；账户模块不提供“显示停用账户”筛选。归档流水不再参与当日或总体汇总。

## 七、界面与交互

系统启动后进入 Today Dashboard，只显示设备本地今天的状态和各领域摘要，不在今日页放置月历、历史日期切换或日期标记表单。左侧导航提供独立的“日历”入口；尚未实现的入口不应造成误导。

桌面主内容区应根据浏览器可用宽度铺满侧栏之外的空间，通过 CSS 视口断点在超宽屏、普通桌面、平板和窄屏之间自动调整列数、留白与导航形态，不以固定最大宽度在右侧留下大块无内容区域。超宽屏通过模块分栏控制单行阅读长度，窄屏保持单列且不得产生横向溢出。

独立日历页承担日期导航、任意日期详情和日期属性管理。页首“回到今日”与 Today 页“查看日历”互为模块导航，点击后返回“今日”页面；月历模块中部另设“回到今天”，用于把月历、所选日期、完整详情和地址栏日期同步定位到设备系统当前日期。日历还应支持连续访问过往及未来月份、通过月份选择器直接跳转年月、上月、下月、选择日期和突出今天。选择过去日期时，页面应按与 Today Dashboard 一致的模块完整呈现当天任务、习惯、健康与睡眠、日记和财务记录；选择未来日期时，应呈现已有安排、启用习惯和空记录状态，并允许快速创建安排到该日的任务。日期数字应清楚突出，并在下方显示农历。日期格必须用可辨识且不只依赖颜色的方式显示工作日、休息日、法定节假日、调休上班、节假日名称、自定义标签和是否存在数据；调休工作日应与普通周末休息日明确区分。v0.1 还可显示习惯完成数量或简单状态点，不要求完整事件日历。

当天详情按任务、习惯、健康/睡眠、财务和日记组织。习惯切换与任务状态立即保存；健康字段在失焦或短暂防抖后保存；财务流水只在用户明确提交后写入。任何保存失败都必须给出可见提示，并保留用户尚未成功提交的输入以便重试。

任务页与习惯页初始只呈现清单和“新建”按钮，不常驻展示新增表单。点击按钮后使用独立的页面内模态窗口承载新增或编辑内容；窗口支持关闭、取消、Escape 和焦点恢复，并在保存成功后自动关闭。该窗口不另起第二个 pywebview 原生窗口，以保持单实例、主题和 API 状态一致。

共享导航应提供界面风格选择器，七套主题的具体设计以 [[界面主题设计规范]] 为准。切换必须即时生效、无需刷新、不发起业务 API 请求，也不得修改 SQLite、schema 或领域状态。主题只允许改变色彩、字体、圆角、边框、阴影、背景纹理、图标描边和局部装饰；页面语义、内容顺序、表单字段、状态文字和键盘访问路径保持一致。主题偏好在桌面模式保存于 WebView 缓存，清空缓存后允许回到默认。

所有主题共用指定 Logo。默认与三套古风主题分别形成现代东方极简、竹青纸本、藏青册页与水色雾面语言；新拟物派以同色基面和内外阴影表达层级；macOS 毛玻璃以半透明、模糊和系统字体表达层级；吉卜力风格只采用温暖自然色、天空、云团、森林色与手绘感组件，不复制具体角色、场景或影视素材。主题差异不得削弱焦点、错误、选中、休息日和保存状态的可辨识性。

Settings 至少提供习惯管理、运行时目录信息、手动备份、数据导出、版本与系统信息。运行时目录可以显示和复制，但切换目录必须在重启前完成校验，避免运行中搬移数据库。

## 八、API 约定

所有接口使用 `/api` 前缀，并返回一致的 JSON 包装：

```json
{
  "success": true,
  "data": {}
}
```

失败时返回适当的 HTTP 状态码与可理解的错误信息；详细异常写入日志，不向前端暴露 Python traceback。

v0.1 至少提供：

```text
GET    /api/day/{date}
GET    /api/calendar/month/{year-month}
PUT    /api/calendar/days/{date}
DELETE /api/calendar/days/{date} # 归档显式标记并恢复默认推断
GET    /api/habits
POST   /api/habits
PUT    /api/habits/{id}
PUT    /api/habits/{id}/log/{date}
GET    /api/tasks
POST   /api/tasks
PUT    /api/tasks/{id}
DELETE /api/tasks/{id}          # 默认执行归档或软删除
GET    /api/health/{date}
PUT    /api/health/{date}
GET    /api/journal/{date}
PUT    /api/journal/{date}
GET    /api/finance/accounts
POST   /api/finance/accounts
PUT    /api/finance/accounts/{id}
GET    /api/finance/transactions
POST   /api/finance/transactions
PUT    /api/finance/transactions/{id}
DELETE /api/finance/transactions/{id}
GET    /api/finance/summary
POST   /api/system/backup
POST   /api/system/export
```

`GET /api/day/{date}` 应一次返回当天日期分类/标记及其他主要数据，降低前端编排复杂度。修改接口需要校验字段、日期和数值范围；数据库写入必须正确执行 commit/rollback。

## 九、数据完整性与生命周期

- `daily_health.date`、`journals.date` 和 `(habit_logs.habit_id, habit_logs.date)` 设置数据库唯一约束。
- 财务账户引用、流水类型与转入/转出组合应由外键、CHECK 约束和服务层同时保护。
- 外键、非空字段、枚举值和合理数值范围应在数据库或服务层得到验证。
- 重要记录默认采用 `active`、`archived_at` 或 `deleted_at` 等软删除机制。
- 数据库连接和事务不得依赖浏览器页面保持打开；正常关闭、异常请求和保存失败都不得留下半写入状态。
- UI、Python 实现和框架均可替换，但数据库结构、迁移规则与导出格式需要保持可演进和向后兼容。

## 十、备份、导出与恢复

### 10.1 备份

程序每天首次启动时自动创建一次 SQLite 一致性备份，也允许用户手动触发。备份保存在 `LIFE_OS_HOME/backups/`，文件名包含日期和必要的时间信息，默认保留最近 30 份；保留数量可配置。

备份应使用 SQLite backup API 或等效的一致性方案，不能在数据库写入期间简单复制单个 `life.db`。备份失败必须记录日志并在界面提示，但不应破坏主数据库。

### 10.2 导出

“Export All Data” 至少生成：

```text
exports/<timestamp>/
├── calendar_days.csv
├── habits.csv
├── habit_logs.csv
├── tasks.csv
├── health.csv
├── finance_accounts.csv
├── finance_transactions.csv
└── journal/
    └── YYYY-MM-DD.md
```

CSV 使用 UTF-8；为兼容 Windows Excel，可使用 UTF-8 BOM。财务金额的导出精度与 API 一致，账户和流水保留可追溯的稳定标识。日记以按日期命名的 Markdown 文件导出。可额外生成包含上述内容的 ZIP，但不是 v0.1 的硬性要求。导入已有 CSV 暂不纳入 v0.1，但导出字段和日期格式应稳定并写入 README。

恢复流程至少支持使用备份数据库替换受损数据库，并在替换前检查文件有效性、保留当前数据库的安全副本。正常界面中采用“安排恢复、正常关闭、下次启动前离线替换”的流程；主数据库损坏导致界面无法打开时，桌面入口和源码诊断入口提供关闭状态下的 `--restore` 恢复参数。恢复与目录迁移的操作说明必须经过实际验证。

## 十一、安全与隐私

- 默认监听 `127.0.0.1`，不得默认监听 `0.0.0.0`。
- v0.1 不实现账户、密码、OAuth、JWT 或多用户权限系统。
- 默认不允许局域网或互联网访问；如果未来开放网络访问，必须另行设计身份认证、传输安全与权限控制。
- 日志不得记录日记正文、健康详情等敏感业务内容；只记录启动、异常、备份、导出和必要的诊断信息。
- 应避免将个人数据发送给遥测、错误上报或第三方资源服务。

## 十二、启动、文档与质量要求

正式发行提供 PyInstaller `onedir` 形式的 `LifeOS.exe`。Windows 普通用户双击后直接进入 pywebview 桌面窗口，不显示终端、不打开外部浏览器，也不要求目标设备预装 Python、pip 或虚拟环境。程序内部使用 Waitress 在随机可用的 `127.0.0.1` 端口提供同源页面与 API，并在端口、WebView2 或数据目录不可用时显示明确错误。

保留 `start.bat` 作为源码开发与故障诊断入口：它可以检查 Python 与依赖，使用固定或显式指定的本地端口启动服务，并按需打开外部浏览器，但不得作为普通用户的最终发行入口。

README 至少说明：系统用途、支持平台、桌面发行包所需的 WebView2 Runtime、源码开发所需的 Python 版本、安装与升级方式、启动与关闭方式、`LIFE_OS_HOME` 的默认位置和修改方法、目录迁移、备份、恢复、导出、项目结构以及已知限制。

`requirements.txt` 只包含实际使用并固定合理版本范围的依赖。代码应命名清晰、避免重复和无意义注释；关键数据操作需有必要说明。日志应支持大小或数量轮转，避免长期运行后无限增长。

自动化测试至少覆盖：

- 首次启动、目录初始化和数据库 schema 初始化
- 自定义 `LIFE_OS_HOME`，以及禁止向该目录之外写入应用状态
- 习惯创建与每日记录的唯一性
- 任务创建、完成、取消和归档
- 健康数据写入与更新
- 日记自动保存所调用的后端写入
- 工作日/休息日推断、显式覆盖、节假日与自定义标签及归档恢复
- 备份的一致性与保留策略
- CSV/Markdown 导出及中文编码
- 程序重启后的数据持久性
- 常见 API 校验错误与事务回滚
- 七套主题选项、主题值白名单、本地 Logo、无业务 API 调用和冻结包静态资源完整性

## 十三、v0.1 范围

v0.1 必须完成：日历导航、工作日/休息日与节假日/自定义标记、Today Dashboard、七套可切换界面主题、统一 Logo、任务、动态习惯、健康、睡眠、体重、日记、当日记账与个人总体财务摘要、SQLite 持久化、统一运行时目录、自动与手动备份、全量导出、Windows 便携桌面发行包、源码诊断启动脚本及核心测试。

v0.1 不实现：AI 分析、财务预算/账单自动导入/投资收益分析等复杂财务能力、其他复杂统计、多币种换算、云同步、账户系统、局域网访问、手机 App、通知、重复任务、子任务界面、日记标签、复杂富文本编辑器和完整数据导入。

后续版本可按价值逐步加入：习惯与健康趋势、连续完成天数、热力图、搜索、标签、周期复盘，以及通过统一只读数据服务生成 Weekly Review、Monthly Review 和行为相关性分析。AI 默认只读；任何数据修改仍必须经过现有服务层和校验规则。

## 十四、开发顺序

1. 建立项目骨架、统一运行时目录解析、配置和 SQLite 初始化。
2. 实现模型、约束、事务处理与核心服务。
3. 实现按日期聚合的数据 API 及各模块 API。
4. 实现基础页面、月历、Today Dashboard、统一 Token、主题切换与本地 Logo。
5. 建立 Waitress + pywebview 桌面运行时、PyInstaller `onedir` 构建、主题资源收集和便携数据目录验证。
6. 完成任务、习惯、健康、睡眠、日记和财务交互与保存。
7. 完成备份、导出、恢复说明和源码诊断启动脚本。
8. 执行自动化测试、桌面干净环境测试、迁移演练和数据恢复验证。
9. 在核心流程稳定后再优化 UI。

每个阶段结束时都应运行现有测试，并通过最小手工流程验证该阶段的数据能够在关闭和重启后保留；未修复阻断性错误前不进入下一阶段。

## 十五、v0.1 验收标准

满足以下场景即视为 MVP 核心合格：

1. 用户双击 `LifeOS.exe` 后直接看到独立桌面窗口，不出现终端或外部浏览器；目标设备无需安装 Python，且外部设备不能直接访问内部服务。
2. 用户选择任意日期，能够新增任务、切换习惯、录入体重、睡眠、精力、情绪和 Markdown 日记。
3. 用户能够把普通工作日标记为休息日、把周末标记为调休工作日，并添加节假日名称或自定义标签；关闭和重启后标记保持不变。
4. 用户能够在当日新增收入、支出或转账，并查看账户余额、总资产、总负债、净资产和指定期间收支。
5. 用户关闭 Life OS 窗口后重新启动，所有已确认保存的数据保持完整且不重复；前一次服务端口、数据库连接和运行时锁均已释放。
6. 自动备份与手动备份均能生成可打开的一致性数据库副本，并按配置执行保留策略。
7. 全量导出能生成结构正确、中文可读的 CSV 与按日期组织的 Markdown 日记。
8. 将程序正常关闭后，用户只复制整个 `LIFE_OS_HOME` 到另一台装有兼容 WebView2 Runtime 的 Windows 设备，配合相同或兼容版本的便携程序目录即可看到原有数据、配置及应用缓存；旧设备绝对路径不会导致失败。
9. 在自定义运行时目录下执行完整流程时，Life OS 不在该目录之外写入数据库、备份、导出、日志、临时文件、pywebview/WebView2 存储或其他应用缓存。
10. 保存、备份或导出失败时，界面提供明确错误，日志记录可诊断信息，主数据库不被破坏。
11. 用户可在七套主题间即时切换；切换和重启不改变任何业务数据，桌面模式可恢复主题偏好，清空 WebView 缓存后安全回退默认，所有主题继续满足键盘、窄屏和状态辨识要求。

## 十六、已确定的默认决策

- 后端：Python + Flask
- ORM：SQLAlchemy
- 数据库：SQLite
- 前端：HTML + CSS + Vanilla JavaScript
- 界面主题：七套主题共用语义 DOM、业务脚本与 REST API，以 `data-theme`、CSS Custom Properties 和本地界面偏好切换
- 项目 Logo：`design-assets/logo-concepts/life-os-logo-02a-ref-a-full-landscape-contained.png`
- 桌面宿主：pywebview；Windows 渲染运行时为 Microsoft Edge WebView2
- 正式本地服务：Waitress；Flask 开发服务器仅用于源码调试
- Windows 构建：PyInstaller `onedir`；不采用 `onefile` 作为 v0.1 默认发行形态
- 开发诊断：保留 `start.bat` 与外部浏览器路径，不作为普通用户入口
- 运行范围：localhost，仅本机访问
- 运行时根目录：`LIFE_OS_HOME`，默认 `./life-os-data/`
- 日历日期类型：工作日/休息日；节假日名称和自定义标签独立保存，显式设置覆盖星期推断
- 日记：一天一篇，支持 Markdown
- 习惯：v0.1 以完成/未完成为主，数据模型支持数值
- 任务：支持无日期任务，预留子任务但暂不提供界面
- 情绪、精力与睡眠质量：1–5 分
- 财务：CNY、整数分存储；收入/支出/转账；账户余额动态计算
- 备份：每日首次启动自动执行，默认保留 30 份
- 导出：CSV + Markdown
- 统计：v0.1 只保留入口或最小摘要，不实现完整分析页

以上决策可在开发前调整；若未提出变更，即作为 v0.1 的实现与验收基线。
