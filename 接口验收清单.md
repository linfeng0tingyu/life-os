---
title: "Life OS v0.1 接口验收清单"
type: acceptance-checklist
created: 2026-09-18T14:17:59+08:00
updated: 2026-09-18T14:42:10+08:00
status: draft
related:
  - "[[项目说明]]"
  - "[[个人生活管理系统 Life OS v0.1 工程需求提纲]]"
  - "[[数据库模型]]"
  - "[[里程碑计划]]"
---

# Life OS v0.1 接口验收清单

## 验收口径

本清单是 v0.1 API 的可测试合同。勾选一项表示该行为已有自动化测试且测试通过，而不是仅凭手工调用观察成功。接口路径以 `/api` 为统一前缀；除明确说明外，请求和响应均使用 JSON。

成功响应统一为：

```json
{
  "success": true,
  "data": {}
}
```

失败响应统一为：

```json
{
  "success": false,
  "error": {
    "code": "validation_error",
    "message": "可供用户理解的错误说明",
    "details": {}
  }
}
```

## 通用合同

- [ ] 所有接口使用正确的 HTTP 方法和状态码；未知路径返回 404，不伪装成成功响应。
- [ ] 成功与失败响应符合统一包装，`Content-Type` 为 `application/json`。
- [ ] 非法 JSON 返回 400，错误字段或不支持的媒体类型返回 400/415，不触发数据库写入。
- [ ] 日期严格接受真实存在的 `YYYY-MM-DD`；例如 `2026-02-30` 返回 400。
- [ ] 未知请求字段按统一策略拒绝或忽略；v0.1 建议拒绝并在 `details` 中指出字段名。
- [ ] 记录不存在返回 404；唯一性或状态冲突返回 409；内部异常返回 500。
- [ ] 任何失败写请求完整回滚，不产生部分更新或重复记录。
- [ ] 响应不包含 Python traceback、本机绝对路径、数据库连接信息、日记正文之外的额外敏感内容。
- [ ] 日志包含请求关联信息和可诊断错误，但不记录日记正文、健康详情或完整请求体。
- [ ] 重复提交幂等的 PUT 请求得到同等最终状态，不产生重复每日记录。
- [ ] 默认服务只绑定 `127.0.0.1`；接口不依赖云服务、浏览器 LocalStorage 或第三方网络资源。

## 日期聚合

### `GET /api/day/{date}`

- [ ] 合法日期返回 200，`data` 至少包含 `date`、`habits`、`tasks`、`health` 和 `journal`。
- [ ] 完全没有数据的日期仍返回 200 和稳定的空结构，而不是 404。
- [ ] `habits` 包含当天可记录习惯及其日志状态；不存在日志时返回未完成的可预测表示。
- [ ] `tasks` 至少区分当天计划、当天到期和此前未完成，并默认排除已归档任务。
- [ ] `health` 和 `journal` 在无记录时返回 `null` 或约定空对象；一经确定不得在不同日期间改变形状。
- [ ] 切换日期不会返回其他日期的健康、日记或习惯日志。
- [ ] 非法日期返回 400，且错误信息指出日期格式或日历值问题。

## 习惯

### `GET /api/habits`

- [ ] 默认只返回启用习惯，并按 `sort_order`、`id` 稳定排序。
- [ ] `include_inactive=true` 时返回停用习惯；非法布尔参数返回 400。
- [ ] 空列表返回 200 和 `[]`。

### `POST /api/habits`

- [ ] 提供有效 `name` 可创建习惯，返回 201 和数据库生成的 `id`。
- [ ] 名称会去除首尾空白；空名称返回 400。
- [ ] 未提供可选字段时应用明确默认值：`active=true`、`sort_order=0`。
- [ ] 客户端提交 `id`、`created_at` 等只读字段时按通用策略拒绝。

### `PUT /api/habits/{id}`

- [ ] 可更新名称、说明、图标、分类、启用状态和排序。
- [ ] 停用后默认列表不再返回该习惯，但既有 `habit_logs` 保持不变。
- [ ] 重新启用使用原 `id` 和历史记录。
- [ ] 不存在的 `id` 返回 404；非法字段不产生部分更新。

### `PUT /api/habits/{id}/log/{date}`

- [ ] 首次写入创建当天日志并返回 200 或 201；合同需固定实际选择。
- [ ] 对相同习惯和日期重复 PUT 执行更新，不产生第二条记录。
- [ ] 支持 `status`，并可选支持 `value`、`value_unit` 和 `note`。
- [ ] 不存在的习惯返回 404；停用习惯是否允许补录必须明确，v0.1 默认允许历史日期补录。
- [ ] 非法日期、非法布尔值或非数值 `value` 返回 400。

## 任务

### `GET /api/tasks`

- [ ] 无筛选时返回未归档任务，并采用稳定排序。
- [ ] 支持 `date`、`status` 和 `include_archived` 筛选；组合筛选结果正确。
- [ ] `date` 筛选同时覆盖计划于该日和到期于该日的定义需在响应中保持可区分。
- [ ] 非法枚举或日期参数返回 400。

### `POST /api/tasks`

- [ ] 仅提供有效 `title` 即可创建无日期任务，返回 201。
- [ ] 可设置描述、状态、优先级、分类、计划日期、到期日期、排序和父任务。
- [ ] 空标题、非法状态、非法优先级或非法日期返回 400。
- [ ] 不存在的 `parent_id` 返回 400/404；自引用和父子循环被拒绝。
- [ ] 初始状态为 `done` 时设置 `completed_at`，其他状态不设置。

### `PUT /api/tasks/{id}`

- [ ] 可更新所有允许编辑的业务字段，未提供字段保持原值。
- [ ] 状态进入 `done` 时设置 `completed_at`；离开 `done` 时按模型规则清空。
- [ ] 任务不能成为自身或其后代的子任务。
- [ ] 不存在或已归档任务的更新行为明确；v0.1 默认已归档任务需显式恢复后才能编辑。
- [ ] 校验失败时原记录所有字段保持不变。

### `DELETE /api/tasks/{id}`

- [ ] 执行软删除/归档并返回 200 与归档结果，不物理删除数据库行。
- [ ] 归档后默认任务查询和日期聚合不再返回该任务。
- [ ] 使用 `include_archived=true` 仍可查询该记录。
- [ ] 对已归档任务重复调用具有幂等结果；不存在的 `id` 返回 404。

## 健康与睡眠

### `GET /api/health/{date}`

- [ ] 存在记录时返回完整健康对象；无记录时返回 200 和 `data: null`。
- [ ] 时间包含日期和 UTC 偏移，分钟数与数据库最终值一致。
- [ ] 非法日期返回 400。

### `PUT /api/health/{date}`

- [ ] 首次 PUT 创建记录，后续 PUT 更新同一条记录，不产生重复日期。
- [ ] 允许仅填写一个健康字段，未提供字段不会被意外清空。
- [ ] `sleep_quality`、`energy_level`、`mood_level` 仅接受 1–5 或 `null`。
- [ ] 体重必须为正数，运动分钟不得为负，非法值返回 400。
- [ ] 同时提供睡眠起止时间时正确计算同日和跨午夜时长。
- [ ] 用户手工设置睡眠分钟数后，`sleep_duration_manual` 与覆盖规则正确。
- [ ] 清空可选字段需使用约定的 `null`，并能与“未提供该字段”区分。

## 日记

### `GET /api/journal/{date}`

- [ ] 存在日记时返回 Markdown 原文和更新时间；无记录时返回 200 和 `data: null`。
- [ ] 不对 Markdown 内容做有损转换；前端渲染时另行执行安全处理。
- [ ] 非法日期返回 400。

### `PUT /api/journal/{date}`

- [ ] 首次 PUT 创建，后续 PUT 更新同日记录；数据库始终只有一行。
- [ ] 接受空字符串并按模型规则保存；缺少 `content` 字段返回 400。
- [ ] 中文、换行、常见 Markdown 和 Unicode 字符往返后保持一致。
- [ ] 快速连续保存时最终内容是最后一次已成功请求的内容，旧响应不会覆盖新状态。
- [ ] 模拟数据库失败时返回错误，原内容保持不变。

## 系统信息、备份与导出

### `GET /api/system/health`

- [x] 服务正常时返回 200、`success=true`、`status=ok`、应用版本和带偏移的当前时间。
- [x] 响应确认运行时已初始化，但不暴露 `LIFE_OS_HOME` 绝对路径或配置内容。
- [x] 实际服务仅监听 `127.0.0.1`，并通过独立临时运行时目录完成 HTTP 调用验证。
- [x] 未知 `/api` 路径返回统一 JSON 失败包装和 404。

### `GET /api/system/info`

- [ ] 返回应用版本、schema 版本、运行时目录状态和备份/导出能力。
- [ ] 路径按隐私规则返回可理解信息；不向不需要的调用暴露其他本机路径。
- [ ] 不返回环境变量、密钥、完整配置或数据库连接字符串。

### `POST /api/system/backup`

- [ ] 使用 SQLite 一致性备份机制生成可打开的数据库副本。
- [ ] 备份位于 `LIFE_OS_HOME/backups/`，文件名不会因同日多次手动备份冲突。
- [ ] 响应返回相对于 `LIFE_OS_HOME` 的备份路径、时间和结果，不返回无关绝对路径。
- [ ] 备份通过 `PRAGMA integrity_check` 或等效检查。
- [ ] 达到保留上限时只清理超额备份，不删除主数据库或保留范围内文件。
- [ ] 目录不可写、磁盘不足或备份失败时返回明确错误，主数据库仍可正常读取。

### `POST /api/system/export`

- [ ] 在 `LIFE_OS_HOME/exports/<timestamp>/` 生成全量导出。
- [ ] 生成 `habits.csv`、`habit_logs.csv`、`tasks.csv`、`health.csv` 和按日期命名的 Markdown 日记。
- [ ] CSV 表头稳定、字段完整，使用 UTF-8；采用 BOM 时所有 CSV 保持一致。
- [ ] Markdown 文件内容与数据库一致，文件名只使用有效日期和安全字符。
- [ ] 导出不修改业务数据，重复导出不会覆盖既有目录。
- [ ] 部分文件写入失败时返回失败状态并标记或清理不完整导出，不能报告成功。

## 并发、重启与迁移验收

- [ ] 两个应用进程尝试使用同一 `LIFE_OS_HOME` 时，第二个进程明确失败或进入只读保护状态。
- [ ] 同一习惯日志、健康记录或日记的并发写入不绕过唯一约束；冲突结果可预测。
- [ ] 正常关闭后重新启动，所有成功响应对应的数据仍可读取。
- [ ] 将关闭后的完整 `LIFE_OS_HOME` 复制到不同绝对路径，所有业务接口返回原有数据。
- [ ] 自定义运行时目录下完成全套接口测试后，目录之外没有 Life OS 生成的数据库、备份、导出、日志、临时文件或应用缓存。

## 完成判定

M3 结束时应通过日期聚合、习惯、任务、健康、日记和系统信息的必选接口项；备份与导出项可在 M7 完成。M8 结束前，本清单所有与 v0.1 范围一致的项目必须勾选，并保存自动化测试结果与迁移/恢复演练记录。未勾选项目应有明确缺陷编号或范围变更记录，不能仅以“手工看起来正常”代替验收。
