---
title: "Life OS 部署与迁移指南"
type: operations-guide
created: 2026-09-18T14:30:00+08:00
updated: 2026-09-22T11:53:25+08:00
status: draft
related:
  - "[[README]]"
  - "[[项目说明]]"
  - "[[个人生活管理系统 Life OS v0.1 工程需求提纲]]"
  - "[[桌面封装架构与改造工作量评估]]"
  - "[[M4.5桌面封装建设记录]]"
  - "[[界面主题设计规范]]"
  - "[[M7数据保护与迁移建设记录]]"
---

# Life OS 部署与迁移指南

> [!important] 当前状态
> M4.5 桌面发行基础和 M7 数据保护已经实施。普通用户可双击 PyInstaller `onedir` 发布目录中的 `LifeOS.exe`，无需保持终端或外部浏览器；“设置与数据”可完成备份、导出与重启恢复。当前构建已通过本机冻结态、跨目录迁移和恢复演练，全新物理设备的最终发布候选验收保留到 M8。

## 迁移模型

Life OS 将“程序”和“可迁移状态”明确分开：桌面发行目录包含 `LifeOS.exe`、内置 Python 运行时、依赖和只读程序资源；当前源码目录包含 Python 源码、静态资源、依赖清单和诊断启动脚本。两种模式都使用同一个 `LIFE_OS_HOME`，其中包含全部应用生成的数据、配置、备份、导出、日记图片附件、缓存、日志和临时状态。跨设备迁移的核心是复制完整的 `LIFE_OS_HOME`，并使用相同或兼容版本的程序打开它。

源码模式的虚拟环境 `.venv`、pip 下载缓存和开发者外部浏览器缓存与设备及 Python 安装绑定，不属于 Life OS 数据。它们不需要复制，也不能作为业务数据的唯一存储位置。正式桌面模式的 pywebview/WebView2 存储属于应用生成状态，必须显式位于 `LIFE_OS_HOME/cache/webview/`，随运行时目录一起管理和迁移；七套主题的当前选择以 `life-os.theme` 保存在这里，属于可丢弃界面偏好。

源码启动脚本会禁止在源码目录生成 Python `__pycache__`，应用自身需要的缓存统一写入 `LIFE_OS_HOME/cache/`。开发工具可能在程序目录产生被 Git 忽略的测试缓存或覆盖率文件，这些属于可随时重建的开发环境产物，不参与用户数据迁移。桌面 `onedir` 发行包的 `_internal/` 同样属于程序资源，不放入 `LIFE_OS_HOME`，也不与个人数据备份混合。

月历农历由目标设备的 WebView2 在本机离线派生，不写入数据库；国务院已公布的 2025、2026 年法定节假日与调休安排属于随程序版本分发的只读规则，也不写入 `LIFE_OS_HOME`。因此迁移个人数据时仍只需复制完整数据目录，但新旧设备应使用相同或兼容的 Life OS 程序版本，才能保证法定日期解析结果一致。尚未公布的年度安排不会由程序猜测，后续版本会依据国务院办公厅正式通知更新规则。

## 目标设备环境

### 桌面发行环境

- Windows 10 或 Windows 11，64 位系统。
- 一个能够读写程序目录和 `LIFE_OS_HOME` 的普通用户账户；不要求管理员权限。
- 已安装兼容的 Microsoft Edge WebView2 Runtime，供 pywebview 渲染窗口内容；缺失时程序应显示明确安装提示。
- 不需要安装 Python、pip、`.venv`、Node.js、外部浏览器、SQLite 或数据库服务；相关 Python 运行时与依赖随 PyInstaller `onedir` 包分发。
- 目标数据盘应支持普通文件锁和原子替换。NTFS 是推荐文件系统；网络共享盘、云盘实时同步目录和只读介质不作为 v0.1 的受支持运行位置。
- 安全软件需要允许 `LifeOS.exe` 在本机回环地址上通信。Life OS 不需要开放 Windows 防火墙入站端口，因为服务只监听 `127.0.0.1`。
- 当前未运行的基础程序目录约 38.0 MiB，建议至少预留 100 MB 供程序、WebView 缓存与后续版本升级。数据盘除现有 `LIFE_OS_HOME` 大小外，应额外保留数据库数倍空间，以容纳备份和导出。

### 当前源码开发与诊断环境

- 64 位 CPython 3.12 或更高版本。当前工程环境已验证 Python 3.14.6；迁移机建议使用 Python 3.12–3.14 的仍受维护版本。
- 安装时推荐勾选 Python Launcher；若不安装 Launcher，必须将 `python.exe` 加入当前用户 PATH。
- `venv` 和 `pip` 必须可用；python.org 的标准 Windows 安装通常包含二者。
- 首次执行 `start.bat` 时，脚本在程序目录创建新的 `.venv`，并根据 `requirements.txt` 安装 Flask、Flask-SQLAlchemy、Waitress 与 pywebview。
- 首次依赖安装需要连接可用的 Python 包源。日常运行不需要互联网。完全离线部署需要另行准备与目标 Python 版本匹配的 wheel 包，本阶段未提供 wheelhouse。

不要从旧设备复制 `.venv`。即使两台设备的路径和系统版本相同，虚拟环境也可能保存旧 Python 的绝对路径。目标开发设备应依据 `requirements.txt` 重新创建。普通使用桌面发行包时不存在此步骤。

### 窗口、端口与容量

- 正式桌面模式由 pywebview 创建独立窗口，通过 WebView2 显示 Flask 同源提供的 HTML、CSS 和 Vanilla JavaScript ES Modules。
- 桌面模式预绑定随机可用的 `127.0.0.1` 端口并交给 Waitress；端口只用于本次进程，不写入长期配置，用户无需记忆或访问本地网址。
- 当前源码诊断模式仍默认使用 `http://127.0.0.1:5000`，并可通过 `config/settings.json` 的 `server.port` 改为其他 1024–65535 端口。
- 两种模式的 `server.host` 都必须保持 `127.0.0.1`；前端不需 Node.js、npm、CORS、反向代理或独立前端服务。

## `LIFE_OS_HOME` 选择与优先级

运行时目录必须是绝对路径。应用按以下顺序选择：

1. `LifeOS.exe --home "绝对路径"`；在当前源码模式下，`app.py --home` 或 `start.bat` 的第一个参数具有相同含义。
2. 当前进程的 `LIFE_OS_HOME` 环境变量。
3. 可执行文件或源码项目目录旁的 `life-os-data/`。

示例：

```bat
LifeOS.exe --home "E:\PortableData\LifeOS"

rem 当前源码模式
start.bat "E:\PortableData\LifeOS"
```

如需为当前 Windows 用户持久设置目录，可以使用系统“环境变量”界面创建 `LIFE_OS_HOME`，值填写完整绝对路径。修改后需要重新打开命令提示符或重新启动应用。不要把 `%USERPROFILE%` 等未展开变量写入 JSON 配置，也不要把 `LIFE_OS_HOME` 设置为单个文件。

## 运行时目录内容

| 路径 | 用途 | 是否必须迁移 |
|---|---|---|
| `config/settings.json` | 端口、日志、备份等用户配置 | 是 |
| `database/` | SQLite 主数据库及 WAL/SHM 伴随文件 | 是 |
| `backups/` | 自动与手动数据库备份 | 是 |
| `exports/` | CSV、Markdown、ZIP 等导出 | 是 |
| `cache/` | 可重建但随目录统一管理的应用缓存 | 建议 |
| `cache/webview/` | 正式桌面模式的 WebView 缓存、`life-os.theme` 主题偏好、LocalStorage、跨随机端口 Cookie 与用户数据目录 | 建议；界面偏好需保留时迁移 |
| `logs/` | 轮转应用日志 | 建议 |
| `temp/` | 锁文件和临时文件 | 一并复制，但只在关闭后操作 |
| `metadata.json` | 运行时格式版本及创建信息 | 是 |

迁移时应复制整个根目录，不要只复制 `life.db`。正常关闭后，残留的 `temp/life-os.lock` 文件本身无害；互斥依赖操作系统文件锁，而不是依赖删除该文件判断运行状态。

## `settings.json` 配置

首次启动会原子创建 UTF-8 JSON 文件：

```json
{
  "settings_version": 1,
  "server": {
    "host": "127.0.0.1",
    "port": 5000,
    "debug": false,
    "open_browser": true
  },
  "backup": {
    "retention_count": 30
  },
  "logging": {
    "level": "INFO",
    "max_bytes": 2097152,
    "backup_count": 5
  }
}
```

配置含义与允许范围：

| 配置 | 类型与范围 | 说明 |
|---|---|---|
| `settings_version` | 整数，当前必须为 `1` | 配置结构版本 |
| `server.host` | 字符串，必须为 `127.0.0.1` | 禁止意外暴露到局域网 |
| `server.port` | 整数，1024–65535 | 源码诊断模式的本机监听端口 |
| `server.debug` | 布尔值 | 正常使用保持 `false`；即使开启也禁用自动重载 |
| `server.open_browser` | 布尔值 | 源码诊断模式启动后是否打开默认浏览器 |
| `backup.retention_count` | 整数，1–3650 | 自动与手动数据库备份的合计保留数量；默认 30 |
| `logging.level` | `DEBUG`/`INFO`/`WARNING`/`ERROR`/`CRITICAL` | 日志级别 |
| `logging.max_bytes` | 整数，65536–104857600 | 单个日志文件最大字节数 |
| `logging.backup_count` | 整数，1–100 | 轮转日志数量 |

修改 JSON 前先关闭程序，保持合法 JSON 语法和 UTF-8 编码。配置无效时应用会停止启动并说明问题，不会悄悄回退或把文件写到其他目录。

`server.port` 与 `server.open_browser` 仅控制当前源码诊断入口。正式桌面模式固定只监听回环地址、自动选择本次进程的可用端口并创建 pywebview 窗口，不读取这两个字段来决定内部端口或是否打开外部浏览器。保留字段是为了兼容既有配置与诊断流程。

## 备份与保留策略

Life OS 每个自然日第一次成功启动时自动创建 `backups/life-os-YYYYMMDD-auto.db`；同一天重复启动不会产生第二份自动备份。“设置与数据”中的“立即备份”创建带时、分、秒和微秒的手动备份，因此连续操作不会覆盖。两类备份都通过 SQLite backup API 从活动数据库取得一致性快照，写入临时文件后执行 `PRAGMA integrity_check` 和 schema 版本校验，全部通过后才原子改名为正式备份。

备份保留数量默认 30，可在设置页修改为 1–3650。每次成功备份后按时间保留最近数量，只清理 `backups/` 根目录中由 Life OS 管理的自动和手动 `.db`；`backups/restore-safety/` 中的恢复前副本不参与自动清理。备份失败只记录错误和界面反馈，不替换、不截断主数据库。

## 全量导出

设置页“导出全部数据”会先创建临时数据库快照，再生成互相独立的 `exports/export-YYYYMMDD-HHMMSS-ffffff/`，不会覆盖旧导出。内容包括：

```text
export-<timestamp>/
├── calendar_days.csv
├── categories.csv
├── habits.csv
├── habit_logs.csv
├── tasks.csv
├── health.csv
├── finance_accounts.csv
├── finance_transactions.csv
├── journal/YYYY-MM-DD.md
└── manifest.json
```

CSV 统一使用 UTF-8 BOM 和稳定表头；用户文本中可能触发电子表格公式的起始字符会加单引号保护。财务金额导出为 CNY 元并固定两位小数，账户与流水保留稳定 `id` 和关联 `account_id`。日记中的本地图片转换为 Base64 数据 URI，因此每篇 Markdown 可脱离附件目录阅读。默认还会在 `exports/` 生成同名 ZIP；导出任一步失败时删除 `.incomplete` 目录和不完整 ZIP，不报告成功，也不修改业务数据。

## 从备份恢复

### 应用仍可打开

1. 打开“设置”→“从备份恢复”，选择已列出的备份。
2. 输入确认短语“恢复此备份”，点击“安排下次启动恢复”。此时只校验并记录请求，不改写当前数据库。
3. 正常关闭 Life OS，不要在关闭前继续录入需要保留的新数据。
4. 重新打开 Life OS。程序取得 `LIFE_OS_HOME` 实例锁后、连接数据库前再次校验备份，把当前 `life.db` 及存在的 WAL/SHM 移到 `backups/restore-safety/<timestamp>/`，然后安装并复检备份。
5. 回到设置页查看“最近恢复成功/失败”反馈并抽查关键记录。失败时原数据库保持不变或自动回滚。

### 主数据库损坏、界面无法打开

先完全关闭 Life OS，记录 `backups/` 中要使用的文件名。桌面发行执行：

```bat
LifeOS.exe --home "D:\LifeOSData" --restore "life-os-20260922-120000-000000-manual.db"
```

源码诊断模式执行：

```bat
.venv\Scripts\python.exe app.py --home "D:\LifeOSData" --restore "life-os-20260922-120000-000000-manual.db"
```

参数只接受 `backups/` 根目录内由 Life OS 命名的备份文件，不接受绝对路径、父目录跳转或任意 SQLite 文件。恢复同样创建 `restore-safety` 副本；完成后再正常启动应用。若提示另一个实例正在运行，不要删除锁文件绕过检查，应先结束仍在使用该目录的进程。

## 从旧设备迁移到新设备

### 旧设备准备

1. 正常关闭 Life OS 窗口；当前源码模式还需停止终端中的服务进程。
2. 确认没有第二个 Life OS 进程仍在使用同一目录。
3. 在“设置与数据”中执行一次手动备份并确认清单出现；可额外执行全量导出，作为开放格式副本。
4. 复制整个 `LIFE_OS_HOME` 到外部介质或安全传输位置。不要在程序运行时单独复制 SQLite 主文件。
5. 记录使用的 Life OS 程序版本；只有源码模式迁移才需要记录 Python 主次版本。

### 新设备准备

1. 确认 Windows 10/11 x64 与 Microsoft Edge WebView2 Runtime 可用。正式桌面发行无需安装 Python；若仍使用当前源码版本，才需要安装 64 位 Python 3.12+。
2. 获取与数据兼容的 Life OS `onedir` 发布目录，并保持 `LifeOS.exe`、`_internal/`、README 与迁移指南的相对结构不变。数据目录与程序目录可以位于不同磁盘。
3. 将完整 `LIFE_OS_HOME` 复制到最终位置；不要先从该位置启动云同步或双向同步工具。
4. 桌面发行模式运行 `LifeOS.exe --home "新设备上的绝对数据路径"`；当前源码模式运行 `start.bat "新设备上的绝对数据路径"`。
5. 桌面发行首次启动不下载依赖，应能离线打开。当前源码模式才需要等待 `.venv` 与依赖安装；若设备离线，应事先准备依赖包，不要复制旧设备 `.venv` 代替安装。

### 迁移后验证

1. 桌面发行应只显示 Life OS 窗口，不出现终端或外部浏览器；内部服务只能监听 `127.0.0.1`。源码模式继续通过浏览器验证。
2. 在应用内系统信息或诊断日志确认健康状态；源码模式可打开 `http://127.0.0.1:5000/api/system/health` 并确认 `success=true`、`status=ok`。
3. 查看 `logs/app.log`，确认日志写入新 `LIFE_OS_HOME`，且没有旧设备绝对路径导致的错误。
4. 确认 `metadata.json` 未被重新创建，`runtime_format_version` 与程序兼容。
5. 检查 `database/life.db`、schema 版本与关键记录；执行一次应用内手动备份与全量导出，并确认它们写入新路径下的 `backups/` 与 `exports/`。
6. 确认程序目录、用户目录和系统临时目录中没有新生成的 Life OS 数据、日志或应用缓存；WebView 缓存应只存在于 `LIFE_OS_HOME/cache/webview/`。
7. 切换任一非默认主题并重启，确认桌面模式恢复主题；清空 `cache/webview/` 时只会回到默认，不会删除 SQLite 业务数据。

当前程序使用 schema v4。从 schema v1/v2 首次启动时，程序会补齐财务表和 `categories`；从 schema v3 启动时会扩展财务分类作用域，并把任务、习惯和既有财务流水中的非空分类回填为单选选项，保留原有生活数据。高于当前程序支持的 schema 仍会阻断启动。迁移前应关闭程序并保留完整 `LIFE_OS_HOME` 副本，不要手工修改 `schema_meta`。

## 常见失败与处理

### 桌面窗口提示缺少 WebView2 或无法创建

确认设备已安装兼容的 Microsoft Edge WebView2 Runtime，然后重新启动 `LifeOS.exe`。程序不得静默回退到系统外部浏览器。若 WebView2 已存在仍失败，查看 `LIFE_OS_HOME/logs/app.log` 并核对发布包是否完整。

### 源码模式提示 Python 不存在或版本过低

安装 64 位 Python 3.12+，启用 Python Launcher 或 PATH，然后删除未完整创建的 `.venv` 并重新运行 `start.bat`。删除 `.venv` 不会删除 `LIFE_OS_HOME` 数据。

### 源码模式依赖安装失败

检查网络、代理、证书和 Python 包源。可以在项目目录手工执行：

```bat
.venv\Scripts\python.exe -m pip install -r requirements.txt
```

不要将 pip 缓存放入 `LIFE_OS_HOME`；它不是应用状态，失败后可以重新下载。

### 运行时目录不可写

确认当前 Windows 用户拥有目标目录的读取、创建、修改和删除文件权限。不要使用 `C:\Program Files`、只读介质或需要管理员权限的位置。应用不会自动回退到其他目录，以免用户误以为正在使用已迁移的数据。

### 端口被占用

桌面模式应自动选择随机可用的回环端口；若仍报端口错误，查看日志确认安全软件或残留进程是否阻止本机监听。源码诊断模式可关闭占用固定端口的程序，或者在 `config/settings.json` 中选择另一个 1024–65535 端口；修改后重新启动，并使用新端口访问。

### 提示另一个实例正在运行

确认是否已有 Life OS 窗口、`LifeOS.exe` 或源码模式 Python 进程。不要通过删除 `life-os.lock` 绕过互斥，因为操作系统锁仍可能存在。正常结束另一个进程后重试；进程异常结束时，操作系统会释放文件锁。

### 运行时格式不兼容

不要强制修改 `metadata.json`。改用与该数据目录兼容的程序版本，或依据未来版本提供的迁移工具升级。操作前保留完整目录副本。

### 备份或导出失败

先在设置页确认运行时目录，再检查该目录磁盘空间和当前账户的写入权限。查看 `logs/app.log` 中对应的 `Data protection error`，日志只记录操作类型和异常类型，不记录日记正文或健康详情。不要把失败产生的临时文件手工改名为正式备份；修复空间或权限后重新执行。

### 安排恢复后改变主意

不要手工编辑 `temp/pending-restore.json`。在当前程序尚未关闭时，返回设置页点击“取消待恢复计划”；接口会移除待处理请求，下次启动不替换数据库。若程序已经关闭并重新启动，恢复可能已完成，应依据设置页结果和 `backups/restore-safety/` 中的恢复前副本判断下一步。

## 安全边界

v0.1 不支持局域网或互联网访问，不提供用户登录和权限隔离。不得将 `server.host` 改为 `0.0.0.0`。如需远程访问，必须先设计身份认证、TLS、访问控制和备份策略，不能仅修改监听地址。

`LIFE_OS_HOME` 包含个人生活与财务数据，应由用户自行选择合适的磁盘加密、设备登录保护和离线备份方式。应用内备份解决 SQLite 一致性，不等于异地备份：仍建议在 Life OS 正常关闭后，把整个运行时目录或最新导出 ZIP 复制到独立介质。实时同步工具可能在 SQLite 写入期间复制不一致文件，不应把正在运行的 `database/` 当作实时同步源。

主题选择不是业务数据。如果迁移时不需要保留界面偏好，可以在 Life OS 完全关闭后省略或删除 `cache/webview/`；下次启动会重建缓存并使用默认主题。页面 Logo 与主题样式位于程序发行目录，不应从旧数据目录复制或覆盖。
