---
title: "Life OS 部署与迁移指南"
type: operations-guide
created: 2026-09-18T14:30:00+08:00
updated: 2026-09-18T16:34:26+08:00
status: draft
related:
  - "[[README]]"
  - "[[项目说明]]"
  - "[[个人生活管理系统 Life OS v0.1 工程需求提纲]]"
---

# Life OS 部署与迁移指南

## 迁移模型

Life OS 将“程序”和“可迁移状态”明确分开：程序目录包含 Python 源码、静态资源、依赖清单和启动脚本；`LIFE_OS_HOME` 包含全部应用生成的数据、配置、备份、导出、缓存、日志和临时状态。跨设备迁移的核心是复制完整的 `LIFE_OS_HOME`，并在目标设备上使用兼容版本的程序打开它。

虚拟环境 `.venv`、pip 下载缓存和浏览器缓存与设备及 Python 安装绑定，不属于 Life OS 数据。它们不需要复制，也不能作为业务数据的唯一存储位置。

正式启动脚本会禁止在源码目录生成 Python `__pycache__`，应用自身需要的缓存统一写入 `LIFE_OS_HOME/cache/`。开发工具可能在程序目录产生被 Git 忽略的测试缓存或覆盖率文件，这些属于可随时重建的开发环境产物，不参与用户数据迁移。

## 目标设备环境

### 操作系统与权限

- Windows 10 或 Windows 11，64 位系统。
- 一个能够读写程序目录和 `LIFE_OS_HOME` 的普通用户账户；不要求管理员权限。
- 目标数据盘应支持普通文件锁和原子替换。NTFS 是推荐文件系统；网络共享盘、云盘实时同步目录和只读介质不作为 v0.1 的受支持运行位置。
- 安全软件需要允许本机 Python 进程监听回环地址。Life OS 不需要开放 Windows 防火墙入站端口，因为服务只监听 `127.0.0.1`。

### Python 与依赖

- 64 位 CPython 3.12 或更高版本。当前工程环境已验证 Python 3.14.6；迁移机建议使用 Python 3.12–3.14 的仍受维护版本。
- 安装时推荐勾选 Python Launcher；若不安装 Launcher，必须将 `python.exe` 加入当前用户 PATH。
- `venv` 和 `pip` 必须可用；python.org 的标准 Windows 安装通常包含二者。
- 首次执行 `start.bat` 时，脚本在程序目录创建新的 `.venv`，并根据 `requirements.txt` 安装 Flask 与 Flask-SQLAlchemy。
- 首次依赖安装需要连接可用的 Python 包源。日常运行不需要互联网。完全离线部署需要另行准备与目标 Python 版本匹配的 wheel 包，本阶段未提供 wheelhouse。

不要从旧设备复制 `.venv`。即使两台设备的路径和系统版本相同，虚拟环境也可能保存旧 Python 的绝对路径。目标设备应依据 `requirements.txt` 重新创建。

### 浏览器、端口与容量

- 使用支持现代 HTML5/CSS 的 Edge、Chrome 或 Firefox。
- 前端使用由 Flask 同源分发的 HTML、CSS 和 Vanilla JavaScript ES Modules，目标设备不需安装 Node.js、npm 或独立前端服务。
- 默认访问地址为 `http://127.0.0.1:5000`。若 5000 端口被占用，可在迁移完成后修改 `config/settings.json` 中的 `server.port`。
- 配置允许端口范围为 1024–65535；`server.host` 在 v0.1 必须保持 `127.0.0.1`。
- 程序与虚拟环境建议预留至少 300 MB。数据盘除现有 `LIFE_OS_HOME` 大小外，应额外保留数据库数倍空间，以容纳备份和导出；实际容量取决于保留策略和日记规模。

## `LIFE_OS_HOME` 选择与优先级

运行时目录必须是绝对路径。应用按以下顺序选择：

1. `app.py --home "绝对路径"`；`start.bat` 的第一个参数会转换为该选项。
2. 当前进程的 `LIFE_OS_HOME` 环境变量。
3. 程序目录下的 `life-os-data/`。

示例：

```bat
start.bat "E:\PortableData\LifeOS"
```

如需为当前 Windows 用户持久设置目录，可以使用系统“环境变量”界面创建 `LIFE_OS_HOME`，值填写完整绝对路径。修改后需要重新打开命令提示符或重新启动应用。不要把 `%USERPROFILE%` 等未展开变量写入 JSON 配置，也不要把 `LIFE_OS_HOME` 设置为单个文件。

## 运行时目录内容

| 路径 | 用途 | 是否必须迁移 |
|---|---|---|
| `config/settings.json` | 端口、日志、备份等用户配置 | 是 |
| `database/` | SQLite 主数据库及 WAL/SHM 伴随文件 | 是 |
| `backups/` | 自动与手动数据库备份 | 是 |
| `exports/` | CSV、Markdown、ZIP 等导出 | 是 |
| `cache/` | 可重建但随目录统一管理的应用缓存 | 建议 |
| `logs/` | 轮转应用日志 | 建议 |
| `temp/` | 锁文件和临时文件 | 一并复制，但只在关闭后操作 |
| `metadata.json` | 运行时格式版本及创建信息 | 是 |

迁移时应复制整个根目录，不要只复制 `life.db`。正常关闭后，残留的 `temp/life-os.lock` 文件本身无害；互斥依赖操作系统文件锁，而不是依赖删除该文件判断运行状态。

## `settings.json` 配置

首次启动会原子创建 UTF-8 JSON 文件：

```json
{
  "settings_version": 1,
  "server": {
    "host": "127.0.0.1",
    "port": 5000,
    "debug": false,
    "open_browser": true
  },
  "backup": {
    "retention_count": 30
  },
  "logging": {
    "level": "INFO",
    "max_bytes": 2097152,
    "backup_count": 5
  }
}
```

配置含义与允许范围：

| 配置 | 类型与范围 | 说明 |
|---|---|---|
| `settings_version` | 整数，当前必须为 `1` | 配置结构版本 |
| `server.host` | 字符串，必须为 `127.0.0.1` | 禁止意外暴露到局域网 |
| `server.port` | 整数，1024–65535 | 本机监听端口 |
| `server.debug` | 布尔值 | 正常使用保持 `false`；即使开启也禁用自动重载 |
| `server.open_browser` | 布尔值 | 启动后是否打开默认浏览器 |
| `backup.retention_count` | 整数，1–3650 | M7 使用的备份保留数量 |
| `logging.level` | `DEBUG`/`INFO`/`WARNING`/`ERROR`/`CRITICAL` | 日志级别 |
| `logging.max_bytes` | 整数，65536–104857600 | 单个日志文件最大字节数 |
| `logging.backup_count` | 整数，1–100 | 轮转日志数量 |

修改 JSON 前先关闭程序，保持合法 JSON 语法和 UTF-8 编码。配置无效时应用会停止启动并说明问题，不会悄悄回退或把文件写到其他目录。

## 从旧设备迁移到新设备

### 旧设备准备

1. 正常关闭浏览器中的操作并停止 Life OS 进程。
2. 确认没有第二个 Life OS 进程仍在使用同一目录。
3. M7 的应用内备份尚未实现，因此必须在 Life OS 完全关闭后复制整个目录；若已有可验证的离线副本，应在迁移前保留一份。
4. 复制整个 `LIFE_OS_HOME` 到外部介质或安全传输位置。不要在程序运行时单独复制 SQLite 主文件。
5. 记录使用的 Life OS 程序版本和 Python 主次版本。

### 新设备准备

1. 安装符合要求的 64 位 Python，并确认 `py -3 --version` 或 `python --version` 不低于 3.12。
2. 获取与数据兼容的 Life OS 程序版本。数据目录与程序目录可以位于不同磁盘。
3. 将完整 `LIFE_OS_HOME` 复制到最终位置；不要先从该位置启动云同步或双向同步工具。
4. 在程序目录运行 `start.bat "新设备上的绝对数据路径"`。
5. 首次启动等待 `.venv` 和依赖安装完成。若目标设备离线，应事先准备依赖包；不要复制旧设备 `.venv` 代替安装。

### 迁移后验证

1. 浏览器应只能通过 `127.0.0.1` 访问。
2. 打开 `http://127.0.0.1:5000/api/system/health`，确认 `success=true`、`status=ok`。
3. 查看 `logs/app.log`，确认日志写入新 `LIFE_OS_HOME`，且没有旧设备绝对路径导致的错误。
4. 确认 `metadata.json` 未被重新创建，`runtime_format_version` 与程序兼容。
5. 检查 `database/life.db`、schema 版本与关键记录；M7 以后再执行一次应用内备份、导出和恢复抽查。
6. 确认程序目录、用户目录和系统临时目录中没有新生成的 Life OS 数据、日志或应用缓存。

当前程序使用 schema v2。从 schema v1 首次启动时，程序会新增 `finance_accounts` 和 `finance_transactions` 并保留原有生活数据；高于当前程序支持的 schema 仍会阻断启动。迁移前应关闭程序并保留完整 `LIFE_OS_HOME` 副本，不要手工修改 `schema_meta`。

## 常见失败与处理

### 提示 Python 不存在或版本过低

安装 64 位 Python 3.12+，启用 Python Launcher 或 PATH，然后删除未完整创建的 `.venv` 并重新运行 `start.bat`。删除 `.venv` 不会删除 `LIFE_OS_HOME` 数据。

### 依赖安装失败

检查网络、代理、证书和 Python 包源。可以在项目目录手工执行：

```bat
.venv\Scripts\python.exe -m pip install -r requirements.txt
```

不要将 pip 缓存放入 `LIFE_OS_HOME`；它不是应用状态，失败后可以重新下载。

### 运行时目录不可写

确认当前 Windows 用户拥有目标目录的读取、创建、修改和删除文件权限。不要使用 `C:\Program Files`、只读介质或需要管理员权限的位置。应用不会自动回退到其他目录，以免用户误以为正在使用已迁移的数据。

### 端口被占用

关闭占用端口的程序，或者在 `config/settings.json` 中选择另一个 1024–65535 端口。修改后重新启动；访问地址也要使用新端口。

### 提示另一个实例正在运行

确认是否已有 Life OS 窗口或 Python 进程。不要通过删除 `life-os.lock` 绕过互斥，因为操作系统锁仍可能存在。正常结束另一个进程后重试；进程异常结束时，操作系统会释放文件锁。

### 运行时格式不兼容

不要强制修改 `metadata.json`。改用与该数据目录兼容的程序版本，或依据未来版本提供的迁移工具升级。操作前保留完整目录副本。

## 安全边界

v0.1 不支持局域网或互联网访问，不提供用户登录和权限隔离。不得将 `server.host` 改为 `0.0.0.0`。如需远程访问，必须先设计身份认证、TLS、访问控制和备份策略，不能仅修改监听地址。

`LIFE_OS_HOME` 包含个人生活与财务数据，应由用户自行选择合适的磁盘加密、设备登录保护和离线备份方式。实时同步工具可能在 SQLite 写入期间复制不一致文件；正式同步方案需在后续里程碑单独验证。
