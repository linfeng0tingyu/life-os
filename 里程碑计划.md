---
title: "Life OS v0.1 里程碑计划"
type: project-plan
created: 2026-09-18T14:17:59+08:00
updated: 2026-09-21T19:10:44+08:00
status: active
related:
  - "[[项目说明]]"
  - "[[个人生活管理系统 Life OS v0.1 工程需求提纲]]"
  - "[[数据库模型]]"
  - "[[接口验收清单]]"
  - "[[桌面封装架构与改造工作量评估]]"
  - "[[M4.5桌面封装建设记录]]"
  - "[[界面主题设计规范]]"
  - "[[M5任务与习惯闭环建设记录]]"
  - "[[M6健康日记与财务闭环建设记录]]"
---

# Life OS v0.1 里程碑计划

## 计划原则

里程碑按“运行时基础 → 数据模型 → API → 日期界面 → 桌面封装 → 业务交互 → 数据保护 → 发布验收”推进。每个里程碑只有在其出口条件全部满足后才能关闭；未修复的阻断性缺陷不得带入下一阶段。界面开发不能先于数据合同，备份与恢复不能留到发布后验证。

## 总览

| 里程碑 | 目标 | 主要结果 | 依赖 | 状态 |
|---|---|---|---|---|
| M0 | 固化项目基线 | 需求、模型、接口与里程碑文档 | 无 | 已完成 |
| M1 | 建立工程与可移植运行时基础 | 可启动骨架、`LIFE_OS_HOME`、配置与目录初始化 | M0 | 已完成 |
| M2 | 建立数据库与领域服务 | 表结构、约束、事务、初始化与模型测试 | M1 | 已完成 |
| M3 | 完成核心 API | 日期聚合及各领域读写接口 | M2 | 已完成 |
| M4 | 完成基础界面与日期导航 | Today Dashboard、月历、保存状态框架、七套主题与统一 Logo | M3 | 已完成 |
| M4.5 | 完成桌面封装与便携发行基础 | pywebview 窗口、Waitress 生命周期、主题偏好归位、PyInstaller onedir | M4 | 已完成 |
| M5 | 完成任务与习惯闭环 | 动态习惯、任务管理、即时保存 | M4.5 | 已完成 |
| M6 | 完成健康、日记与财务闭环 | 健康/睡眠、日记自动保存、记账与财务摘要 | M4.5 | 已完成 |
| M7 | 完成数据保护与可迁移性 | 自动备份、导出、恢复说明与迁移演练 | M2–M6 | 待开始 |
| M8 | 完成系统验收与 v0.1 发布 | 全量测试、错误处理、文档与发布候选版 | M1–M7 | 待开始 |

## M0：项目基线

### 交付物

- 项目说明与明确的 v0.1 范围边界。
- 工程需求提纲、数据库模型、接口验收清单和本里程碑计划。
- `LIFE_OS_HOME` 的目录边界、迁移原则和最终验收场景。

### 出口条件

- 四份规划文档互相链接且不存在明显冲突。
- 后续开发可以从 M1 开始，不需要重新决定基础技术栈或数据目录原则。

## M1：工程骨架与可移植运行时基础

### 工作范围

- 创建 Python/Flask 项目骨架、依赖清单、基础测试配置和 Windows 启动入口。
- 实现唯一的运行时路径解析模块：读取 `LIFE_OS_HOME`，未设置时使用 `./life-os-data/`。
- 初始化 `config/`、`database/`、`backups/`、`exports/`、`cache/`、`logs/` 和 `temp/`。
- 校验目录权限、端口占用和单实例运行；建立日志轮转与统一错误处理骨架。
- 禁止业务模块自行拼接或硬编码可变数据路径。

### 出口条件

- `start.bat` 能启动最小应用并访问健康检查页面或接口。
- 默认目录和自定义绝对目录均能初始化；不可写目录会明确失败，不会回退到其他位置。
- 自动化测试证明应用状态不会写到 `LIFE_OS_HOME` 之外。
- 项目在无 Docker、无 Node.js、无外部数据库服务的环境中可运行。

### 完成记录

M1 于 2026-09-18 完成。工程已建立 Flask app factory、Windows `start.bat`、项目内 `.venv` 引导、统一运行时路径对象、指向 `LIFE_OS_HOME/database/life.db` 的 Flask-SQLAlchemy 基础配置、原子配置与元数据初始化、轮转日志、跨平台文件锁、端口占用检测、健康检查接口及错误处理骨架。`LIFE_OS_HOME` 的默认和自定义绝对路径均已覆盖测试，应用启动时禁止在源码目录生成 Python 字节码缓存。

验证结果为 13 项 pytest 测试全部通过；`app.py --check` 在独立临时目录生成预期目录与文件；`start.bat` 能完成依赖引导并使本地服务仅监听 `127.0.0.1:5000`，`GET /api/system/health` 返回 200。测试服务与临时运行时目录已清理。目标设备环境、配置格式和迁移步骤见 [[部署与迁移指南]]。

2026-09-18 根据 [[前端设计与技术架构提纲]] 完成 M1 复核：现有 Flask/Jinja 同源页面、`static/` 资源、无 Node.js 启动链、本地依赖和 `LIFE_OS_HOME` 边界均与提纲兼容。已增加可继承的 `base.html`、应用版本资源参数、HTML `no-cache` 响应和本地资源测试，为 M4 的 Vanilla ES Modules、统一 API Client 和 CSS Token 拆分提供稳定入口。详细核查结果见 [[M1前端架构核查记录]]。

2026-09-20 按桌面架构对 M1 运行时作增量修正：冻结模式默认把 `life-os-data/` 定位到 `LifeOS.exe` 同级，而不是 PyInstaller `_internal/`；运行时目录新增 `cache/webview/`，依赖引导同时检查 Waitress 与 pywebview。源码入口、配置、日志和实例锁仍复用原 M1 实现。

2026-09-20 按主题需求复核 M1：现有版本化本地静态资源、无 Node.js 构建链与 `cache/webview/` 已能承载主题 CSS、主题脚本、Logo 和可丢弃偏好，无需增加后端运行服务或新的可变数据目录。

## M2：数据库与领域服务

### 工作范围

- 按 [[数据库模型]] 实现 SQLAlchemy 模型、唯一约束、检查约束、索引和外键。
- 开启 SQLite 外键检查，配置合理的 busy timeout，并明确 WAL 与正常关闭策略。
- 实现首次数据库初始化、schema 版本记录、事务边界和统一时间格式。
- 实现日期分类/标记、习惯、任务、健康、日记与个人财务的最小领域服务；业务规则不放入路由或前端。

### 出口条件

- 空运行时目录启动后自动生成有效数据库，不要求手工执行 SQL。
- 重复每日记录被唯一约束阻止；失败事务完整回滚。
- 核心模型和服务测试全部通过，数据库重启后数据保持一致。
- 数据库文件及 WAL/SHM 伴随文件均位于 `LIFE_OS_HOME/database/`。

### 完成记录

M2 于 2026-09-18 完成，并于同日根据新财务需求扩展。SQLite 首次启动自动建库并写入 `schema_version = 2`；schema v1 启动时自动新增财务表并升级为 v2，不删除既有数据。高版本数据库或存在业务表但缺少版本元数据时停止写入。连接统一启用外键、5 秒 busy timeout、WAL 与 NORMAL synchronous，启动执行完整性检查，正常关闭执行 checkpoint。

已实现 `calendar_days`、`habits`、`habit_logs`、`tasks`、`daily_health`、`journals`、`finance_accounts`、`finance_transactions` 和 `schema_meta` 模型，以及日期分类/标记、习惯、任务、健康/睡眠、日记和财务服务。财务服务以整数分管理 CNY，校验收入/支出/转账账户方向，并从期初余额与流水动态计算账户余额。

原 M2 的 38 项测试保持通过；新增测试覆盖 schema v1→v2 无损升级、金额精度、账户方向、转账、归档和资产/负债/净资产计算。

主题需求不修改 M2。主题不是领域实体，不新增数据库表、字段或迁移，schema 保持 v2；删除 WebView 主题偏好不会影响任何业务记录。

## M3：核心 API

### 工作范围

- 实现统一 JSON 成功/失败包装、参数校验、HTTP 状态码和异常日志。
- 实现日期聚合、月历日期分类/标记、习惯、习惯记录、任务、健康、日记、个人财务与系统信息接口。
- 为所有写接口建立成功、校验失败、记录不存在、冲突和事务回滚测试。
- 完成 [[接口验收清单]] 中 M3 范围内的项目。

### 出口条件

- 无需前端即可通过自动化测试完整创建、读取、更新和归档核心数据。
- `GET /api/day/{date}` 返回稳定的数据形状，并正确聚合当天信息。
- 后端异常不会向客户端泄露 traceback 或敏感内容。
- 所有已实现接口满足通用合同及对应的逐接口验收项。

### 完成记录

M3 于 2026-09-18 完成，并于同日根据新财务需求扩展。除原有核心接口外，现已提供财务账户创建/查询/更新、收入/支出/转账流水查询/写入/编辑/归档和总体财务汇总。`GET /api/day/{date}` 的稳定形状新增 `finance`，包含当日收入、支出、净现金流和流水。

验证结果扩展为 73 项 pytest 测试全部通过。新增覆盖财务账户、流水、按日聚合、期间汇总、精确金额、转账不重复计入收支和归档后重算。备份与包含财务表的全量导出仍属于 M7。

主题需求不修改 M3。切换逻辑不新增接口、不导入 API Client、不调用 `fetch`；全部既有业务读写继续使用统一 REST 合同。

## M4：基础界面与日期导航

### 工作范围

- 建立视口自适应的 Today Dashboard、独立日历/任意日期详情页、共享导航和 Today/Calendar 共用的完整日期详情容器。
- 支持上月、下月、月份直达、返回今天和选择任意过去或未来日期；未来日期允许快速新增当天计划任务。
- 建立前端 API 封装、加载状态、空状态、错误提示与“保存中/已保存/保存失败”状态组件。
- 日期格显示工作日/休息日、节假日名称、自定义标签、当天是否存在数据及最小习惯完成摘要。
- Today Dashboard 为当日收支和快速记账保留稳定容器，不在 M4 提前实现完整财务管理界面。
- 在共享导航提供七套主题选择器，使用同一语义 DOM、CSS Token 和组件状态即时切换；主题偏好仅为可丢弃界面状态。
- 页面品牌使用指定 `life-os-logo-02a-ref-a-full-landscape-contained.png` 的本地静态副本，不再使用 CSS 占位 Logo。

### 出口条件

- 打开系统默认显示本地今天，切换日期不会整页刷新。
- 选择日期后展示对应聚合数据，月份切换和返回今天行为正确。
- 网络或服务错误可见且可重试，不会清空用户尚未成功保存的输入。
- 键盘操作和窄屏下的基本内容访问不受阻断。
- 七套主题都能切换并恢复偏好，不发送业务请求、不修改数据库；Logo 本地加载，主题失效时回退默认。

### 完成记录

M4 于 2026-09-19 完成，并于 2026-09-20 根据界面复核调整信息架构。前端已按 [[前端设计与技术架构提纲]] 拆分为 Token、基础排版、布局、组件及 Today/Calendar 页面样式，以及 API Client、日期工具、DOM 组件、月历功能和页面控制器。Today 固定显示设备本地今天；独立 Calendar 页面支持过去与未来月份、月份选择器直达年月、URL 日期状态、无刷新日期选择、上月/下月、返回今天、未来任务快速规划、旧请求取消和只允许最后一次响应更新页面。

Today Dashboard 已接入日期聚合 API，以只读方式呈现本地今天的任务、习惯、健康节律、日记和财务摘要；Calendar 复用相同完整详情布局，过去日期用于回看，未来日期用于查看已有安排并创建计划任务。月历及工作日/休息日、节假日、自定义标签和备注统一位于 Calendar 页面。主内容随设备视口填满侧栏之外空间，超宽屏自动分栏，窄屏保持单列。共享导航、按钮和月历控件已按 `design-demo` 统一为本地线性 SVG 图标与青黛/暖白配色，不使用字符占位图标。

日期详情中的生活节律概览收敛为睡眠时长、体重、运动和身体健康四项；睡眠时段、质量、精力、情绪和备注等详细字段继续由 M6 的独立生活节律板块承载。

验证结果扩展为 90 项 pytest 测试全部通过。浏览器验收覆盖今日/日历页面分离、过去日期完整回顾、未来计划创建并即时进入日详情、历史标记保存、月份切换、返回今天、按钮图标完整性及 2560px、1920px、1280px、768px、375px 响应式布局；浏览器控制台无错误。详细记录见 [[M4前端建设记录]]。

2026-09-20 完成桌面兼容复核：M2 的 SQLite、领域服务和 checkpoint，M3 的同源 REST API 与健康检查，以及 M4 的模板、静态资源、日期状态和响应式页面均由桌面宿主直接复用，没有产生平行业务入口或第二套前端。冻结包中的 Today 页面已在 WebView2 独立窗口完整呈现。

2026-09-20 完成 M4 主题增量：新增 `themes.css`、`theme.js`、共享主题选择器及本地 Logo，七套视觉方案均按 [[界面主题设计规范]] 独立定义，不读取早期尝试版 HTML。主题通过 `<html data-theme>` 覆盖 Token 与组件装饰；切换不刷新页面、不调用 API，并使用白名单处理损坏偏好。

主题增量完成后，全量自动化测试扩展为 101 项并全部通过，新增覆盖七套选项、主题 CSS、偏好键、无业务 API 调用、Logo PNG 与新增静态资源。

## M4.5：桌面封装与便携发行基础

### 工作范围

- 新增正式桌面入口，以 pywebview 承载现有同源页面，不复制业务逻辑，也不建立新的 JS-Python 业务桥接。
- 使用 Waitress 替代 Flask 开发服务器作为正式本地 WSGI 服务；预先绑定随机可用的 `127.0.0.1` 端口并交给服务线程，避免固定端口冲突和端口探测竞争。
- 建立窗口、服务线程和单实例锁的统一生命周期：服务成功后显示窗口，关闭窗口时停止服务并释放数据库、端口和锁。
- 将 pywebview/WebView2 的缓存与存储显式重定向到 `LIFE_OS_HOME/cache/webview/`，继续遵守单目录可迁移边界。
- 将主题 LocalStorage、跨随机端口 Cookie 与其他 WebView 偏好保存在上述目录，并把指定 Logo、主题 CSS 和主题脚本纳入冻结资源。
- 建立 PyInstaller `onedir` 构建配置、资源收集规则和可重复的 Windows 构建脚本；保留 `start.bat` 作为源码开发与故障诊断入口。
- 覆盖源码模式、冻结模式、缺少 WebView2、端口占用、重复启动、异常退出、含中文路径以及运行时目录迁移场景。

### 出口条件

- 用户双击 `LifeOS.exe` 后只出现 Life OS 窗口，不出现终端，也不依赖外部浏览器。
- 正式发行包在未安装 Python、pip、虚拟环境、Node.js 和数据库服务的 Windows 10/11 设备上可运行；目标设备只需兼容的 Microsoft Edge WebView2 Runtime。
- 本地服务只监听 `127.0.0.1`，动态端口不与其他程序冲突；关闭窗口后端口、数据库连接和运行时锁均被释放。
- 数据库、配置、日志、导出、临时文件以及 WebView 存储全部位于 `LIFE_OS_HOME`；复制运行时目录后数据与界面偏好可恢复。
- PyInstaller `onedir` 包在干净设备、中文路径和迁移后的新路径上通过验收，并保留可诊断的日志和错误提示。
- 页面 Logo 与 Windows EXE 图标来自同一确认源文件；重启后恢复主题，清空 WebView 缓存后回退默认。

### 完成记录

M4.5 于 2026-09-20 完成。工程新增 `desktop_entry.py`、桌面 Host 与可停止的 Waitress WSGI Host；服务通过预绑定 socket 使用随机 `127.0.0.1` 端口，健康检查成功后创建 pywebview 窗口。窗口关闭时依次停止服务、执行 SQLite checkpoint 并释放实例锁；即使服务停止异常，数据库与锁清理仍会继续。WebView2 用户数据显式写入 `LIFE_OS_HOME/cache/webview/`，未向 JavaScript 暴露 Python 业务桥接。

已建立 `LifeOS.spec`、`requirements-build.txt`、图标生成脚本和 `build_desktop.bat`，生成无控制台的 Windows x64 `onedir` 包，并复制 README 与迁移指南到发行目录。未运行的基础包约 38.0 MiB，产品版本为 `0.1.0.0`，不包含默认 `life-os-data/`。

2026-09-20 完成 M4.5 主题与品牌增量：冻结资源收集继续覆盖新增 CSS、JavaScript 和 PNG；图标生成脚本改为从已确认 Logo 源文件裁切透明边界并生成多尺寸 ICO。桌面主题偏好继续随 `cache/webview/` 管理，不新增数据库或配置 API。

主题增量完成后验证结果为 101 项 pytest 全部通过。源码 `--check`、冻结 `--check`、无 Python PATH、中文数据路径、随机端口启停、锁释放、冻结态默认相邻数据目录均已通过；重建发行包已完成七套主题逐项窗口检查、指定 Logo 验证、跨随机端口偏好恢复和关闭无残留进程验收。全新物理 Windows 设备上的迁移与恢复演练仍归入 M8 最终发布验收。详细记录见 [[M4.5桌面封装建设记录]]。

## M5：任务与习惯闭环

### 工作范围

- 实现习惯新增、编辑、排序、停用、恢复及当天状态即时保存。
- 实现有日期与无日期任务的新增、编辑、完成、取消、归档、优先级和日期调整。
- 在 Dashboard 区分当天计划、当天到期和此前未完成任务。
- 防止重复点击或请求重试生成重复习惯日志。

### 出口条件

- 任务与习惯的主要用户流程均通过界面和 API 验收。
- 页面刷新及程序重启后状态保持一致。
- 归档记录不再出现在默认列表，但仍可通过明确参数查询。
- 快速重复操作不会生成重复每日记录或错误的任务状态。

### 完成记录

M5 于 2026-09-20 完成。新增独立 `/tasks` 与 `/habits` 页面：任务支持有日期/无日期创建、编辑、开始、完成、取消、重新打开、优先级与日期调整、父任务选择、排序、归档、显示已归档和恢复；习惯支持创建、编辑、分类与图标、排序、停用、显示已停用、恢复，以及今天和过去日期的即时打卡。主导航已开放两个管理入口，Today 与 Calendar 的共用日期详情也增加任务状态和习惯完成状态的快捷操作。

2026-09-21 完成 M5 交互增量：任务与习惯新增/编辑表单改为默认关闭的页面内模态窗口；新增 schema v3 `categories` 表和分类接口，使两类分类成为分作用域、可现场创建的单选项，并在升级时回填旧数据。页面与 Windows ICO 的 Logo 视觉占比增大，ICO 按可见 Alpha 阈值裁掉近乎透明的杂散边缘。Calendar 页首“回到今日”返回 Today，月历内部新增“回到今天”并等待月历和日期聚合共同完成，修复跨月份定位的异步竞态。

任务归档恢复使用新增的 `POST /api/tasks/{id}/restore`，恢复重复调用保持幂等；默认任务查询和日期聚合仍排除归档记录。习惯打卡继续使用 `(habit_id, date)` 唯一约束和服务层 upsert，并在只切换完成状态时保留既有数值、单位和备注。前端为每个写入目标维护进行中状态并禁用重复触发，失败时保留输入并显示错误。

全量自动化测试扩展为 111 项并全部通过，新增覆盖恢复接口、幂等行为、分类作用域、旧分类迁移、管理页面结构、静态资源、重复操作保护和详细习惯日志保留。界面验收完成任务与习惯分类创建/单选保存、按需弹窗，以及从 2030 年日期返回 2026-09-21 今日状态；原有任务、习惯闭环继续通过。详细记录见 [[M5任务与习惯闭环建设记录]]。

## M6：健康、日记与财务闭环

### 工作范围

- 实现体重、睡眠起止、睡眠时长、睡眠质量、精力、情绪、身体状态、运动分钟和备注。
- 自动计算跨午夜睡眠时长，并允许用户显式修正。
- 实现 Markdown 日记编辑与 1–2 秒防抖自动保存。
- 实现财务账户管理、当日收入/支出/转账记录，以及总资产、总负债、净资产和期间收支摘要。
- 对空值、边界值、非法日期和数值格式提供明确校验反馈。

### 出口条件

- 每日健康和日记均采用 upsert，不产生同日重复记录。
- 睡眠时长计算覆盖同日与跨午夜场景，手工值不会被意外覆盖。
- 日记保存状态准确；失败输入不会丢失，重试成功后可在重启后读取。
- 健康数据允许部分填写，不因无关字段为空而拒绝保存。
- 记账失败时表单内容不丢失；转账不计入收支，账户余额与后端汇总一致。

M6 于 2026-09-21 完成。新增 `/health`、`/journal` 与 `/finance` 三个独立管理页，并把 Today 与 Calendar 的日期详情入口按当前所选日期连接到对应页面。健康页支持全部节律字段的短防抖/失焦自动保存、跨午夜睡眠自动计算和显式手动修正；日记页支持 Markdown 原文、1.2 秒防抖保存、当前 24 小时制时分插入、三级标题、图片附件、安全预览、单日自包含 Markdown 导出、失败保留输入与离开保护；财务页支持资产/负债账户创建、编辑和启停，可现场新增的单选分类，收入/支出/转账创建、编辑和软归档，以及当前余额、资产、负债、净资产和期间收支汇总。完整实现与验收记录见 [[M6健康日记与财务闭环建设记录]]。

## M7：数据保护、导出与迁移

### 工作范围

- 使用 SQLite backup API 或等效一致性机制实现每日首次启动备份和手动备份。
- 实现可配置的备份保留数量，默认保留最近 30 份。
- 实现结构化数据 CSV 导出、全部日记批量 Markdown 导出及可选 ZIP 封装；M6 已有的单日导出继续保留。
- 编写并验证备份恢复、运行时目录迁移和异常中断处理说明。

### 出口条件

- 备份可以打开并通过完整性检查；备份失败不会破坏主数据库。
- 导出目录和文件位于 `LIFE_OS_HOME`，CSV 中文可被常见 Windows 工具正确读取。
- 在正常关闭后复制整个 `LIFE_OS_HOME` 到另一位置，应用能加载原有数据和配置。
- 新设备路径与旧设备不同也不影响运行，数据库和配置中不存在阻断迁移的绝对路径。

## M8：系统验收与 v0.1 发布

### 工作范围

- 完成需求提纲中的端到端验收场景及 [[接口验收清单]] 全部必选项。
- 覆盖启动、关闭、并发请求、事务失败、磁盘不可写、备份失败和导出失败等关键异常。
- 完善 README、安装升级、关闭、迁移、备份、恢复、导出和已知限制说明。
- 生成 PyInstaller `onedir` 桌面发布候选包，并在无 Python 的干净 Windows 环境完成启动、关闭和迁移验收。
- 清理无用依赖、调试输出、敏感日志及误导性的未实现入口。

### 出口条件

- 自动化测试全部通过，手工 MVP 流程、迁移演练和恢复演练均有记录。
- 默认只监听 `127.0.0.1`，日志不包含日记正文或健康详情。
- 新环境可依据 README 直接运行桌面发行包并完成启动、关闭和数据迁移；普通使用不要求安装 Python。
- 无阻断性缺陷；其余已知问题已记录影响、规避方式和后续版本归属。

## 变更控制

开发过程中若新增功能影响数据库结构、公共 API 或 `LIFE_OS_HOME` 边界，应先同步更新需求提纲、数据库模型和接口验收清单，再进入实现。非阻断性的 v0.2 候选不得插入当前里程碑。
