---
title: "Life OS v0.1 里程碑计划"
type: project-plan
created: 2026-09-18T14:17:59+08:00
updated: 2026-09-18T15:19:32+08:00
status: active
related:
  - "[[项目说明]]"
  - "[[个人生活管理系统 Life OS v0.1 工程需求提纲]]"
  - "[[数据库模型]]"
  - "[[接口验收清单]]"
---

# Life OS v0.1 里程碑计划

## 计划原则

里程碑按“运行时基础 → 数据模型 → API → 日期界面 → 业务交互 → 数据保护 → 发布验收”推进。每个里程碑只有在其出口条件全部满足后才能关闭；未修复的阻断性缺陷不得带入下一阶段。界面开发不能先于数据合同，备份与恢复不能留到发布后验证。

## 总览

| 里程碑 | 目标 | 主要结果 | 依赖 | 状态 |
|---|---|---|---|---|
| M0 | 固化项目基线 | 需求、模型、接口与里程碑文档 | 无 | 已完成 |
| M1 | 建立工程与可移植运行时基础 | 可启动骨架、`LIFE_OS_HOME`、配置与目录初始化 | M0 | 已完成 |
| M2 | 建立数据库与领域服务 | 表结构、约束、事务、初始化与模型测试 | M1 | 已完成 |
| M3 | 完成核心 API | 日期聚合及各领域读写接口 | M2 | 已完成 |
| M4 | 完成基础界面与日期导航 | Today Dashboard、月历、保存状态框架 | M3 | 待开始 |
| M5 | 完成任务与习惯闭环 | 动态习惯、任务管理、即时保存 | M4 | 待开始 |
| M6 | 完成健康、睡眠与日记闭环 | 健康记录、睡眠计算、日记自动保存 | M4 | 待开始 |
| M7 | 完成数据保护与可迁移性 | 自动备份、导出、恢复说明与迁移演练 | M2–M6 | 待开始 |
| M8 | 完成系统验收与 v0.1 发布 | 全量测试、错误处理、文档与发布候选版 | M1–M7 | 待开始 |

## M0：项目基线

### 交付物

- 项目说明与明确的 v0.1 范围边界。
- 工程需求提纲、数据库模型、接口验收清单和本里程碑计划。
- `LIFE_OS_HOME` 的目录边界、迁移原则和最终验收场景。

### 出口条件

- 四份规划文档互相链接且不存在明显冲突。
- 后续开发可以从 M1 开始，不需要重新决定基础技术栈或数据目录原则。

## M1：工程骨架与可移植运行时基础

### 工作范围

- 创建 Python/Flask 项目骨架、依赖清单、基础测试配置和 Windows 启动入口。
- 实现唯一的运行时路径解析模块：读取 `LIFE_OS_HOME`，未设置时使用 `./life-os-data/`。
- 初始化 `config/`、`database/`、`backups/`、`exports/`、`cache/`、`logs/` 和 `temp/`。
- 校验目录权限、端口占用和单实例运行；建立日志轮转与统一错误处理骨架。
- 禁止业务模块自行拼接或硬编码可变数据路径。

### 出口条件

- `start.bat` 能启动最小应用并访问健康检查页面或接口。
- 默认目录和自定义绝对目录均能初始化；不可写目录会明确失败，不会回退到其他位置。
- 自动化测试证明应用状态不会写到 `LIFE_OS_HOME` 之外。
- 项目在无 Docker、无 Node.js、无外部数据库服务的环境中可运行。

### 完成记录

M1 于 2026-09-18 完成。工程已建立 Flask app factory、Windows `start.bat`、项目内 `.venv` 引导、统一运行时路径对象、指向 `LIFE_OS_HOME/database/life.db` 的 Flask-SQLAlchemy 基础配置、原子配置与元数据初始化、轮转日志、跨平台文件锁、端口占用检测、健康检查接口及错误处理骨架。`LIFE_OS_HOME` 的默认和自定义绝对路径均已覆盖测试，应用启动时禁止在源码目录生成 Python 字节码缓存。

验证结果为 13 项 pytest 测试全部通过；`app.py --check` 在独立临时目录生成预期目录与文件；`start.bat` 能完成依赖引导并使本地服务仅监听 `127.0.0.1:5000`，`GET /api/system/health` 返回 200。测试服务与临时运行时目录已清理。目标设备环境、配置格式和迁移步骤见 [[部署与迁移指南]]。

## M2：数据库与领域服务

### 工作范围

- 按 [[数据库模型]] 实现 SQLAlchemy 模型、唯一约束、检查约束、索引和外键。
- 开启 SQLite 外键检查，配置合理的 busy timeout，并明确 WAL 与正常关闭策略。
- 实现首次数据库初始化、schema 版本记录、事务边界和统一时间格式。
- 实现日期分类/标记、习惯、任务、健康、日记的最小领域服务；业务规则不放入路由或前端。

### 出口条件

- 空运行时目录启动后自动生成有效数据库，不要求手工执行 SQL。
- 重复每日记录被唯一约束阻止；失败事务完整回滚。
- 核心模型和服务测试全部通过，数据库重启后数据保持一致。
- 数据库文件及 WAL/SHM 伴随文件均位于 `LIFE_OS_HOME/database/`。

### 完成记录

M2 于 2026-09-18 完成。SQLite 首次启动自动建库并写入 `schema_version = 1`；现有数据库版本不兼容或存在业务表但缺少版本元数据时停止写入。连接统一启用外键、5 秒 busy timeout、WAL 与 NORMAL synchronous，启动执行完整性检查，正常关闭执行 checkpoint。

已实现 `calendar_days`、`habits`、`habit_logs`、`tasks`、`daily_health`、`journals` 和 `schema_meta` 模型，以及日期分类/标记、习惯、任务、健康/睡眠、日记服务。服务层负责输入校验、upsert、软归档、任务父子循环防护、睡眠时长计算和事务回滚。

验证结果为 38 项 pytest 测试全部通过，覆盖首次建库、SQLite pragma、schema 不兼容与无版本数据库阻断、外键/唯一/评分约束、重启持久性、事务失败回滚和所有领域服务。独立临时 `LIFE_OS_HOME` 已通过 `app.py --check` 建库验证并在验证后清理。

## M3：核心 API

### 工作范围

- 实现统一 JSON 成功/失败包装、参数校验、HTTP 状态码和异常日志。
- 实现日期聚合、月历日期分类/标记、习惯、习惯记录、任务、健康、日记与系统信息接口。
- 为所有写接口建立成功、校验失败、记录不存在、冲突和事务回滚测试。
- 完成 [[接口验收清单]] 中 M3 范围内的项目。

### 出口条件

- 无需前端即可通过自动化测试完整创建、读取、更新和归档核心数据。
- `GET /api/day/{date}` 返回稳定的数据形状，并正确聚合当天信息。
- 后端异常不会向客户端泄露 traceback 或敏感内容。
- 所有已实现接口满足通用合同及对应的逐接口验收项。

### 完成记录

M3 于 2026-09-18 完成。已建立统一成功/失败 JSON 包装、字段白名单、严格日期与查询参数校验、HTTP 状态码映射和 `X-Request-ID` 关联标识。后端已实现日期聚合、月历及日期标记、习惯与每日记录、任务读写/筛选/归档、健康与睡眠、Markdown 日记和系统信息接口。`GET /api/day/{date}` 始终返回稳定形状，任务明确分为计划、当日到期和此前未完成三组。

验证结果为 63 项 pytest 测试全部通过。测试覆盖所有 M3 接口的正常读写、空结构、幂等更新、归档、组合筛选、校验失败、不存在记录、冲突、异常回滚和错误响应不泄露路径、traceback 或日记内容。备份与导出能力仍按计划属于 M7。

## M4：基础界面与日期导航

### 工作范围

- 建立桌面优先的 Today Dashboard、月历导航、顶部导航和当天详情容器。
- 支持上月、下月、返回今天和选择任意日期。
- 建立前端 API 封装、加载状态、空状态、错误提示与“保存中/已保存/保存失败”状态组件。
- 日期格显示工作日/休息日、节假日名称、自定义标签、当天是否存在数据及最小习惯完成摘要。

### 出口条件

- 打开系统默认显示本地今天，切换日期不会整页刷新。
- 选择日期后展示对应聚合数据，月份切换和返回今天行为正确。
- 网络或服务错误可见且可重试，不会清空用户尚未成功保存的输入。
- 键盘操作和窄屏下的基本内容访问不受阻断。

## M5：任务与习惯闭环

### 工作范围

- 实现习惯新增、编辑、排序、停用、恢复及当天状态即时保存。
- 实现有日期与无日期任务的新增、编辑、完成、取消、归档、优先级和日期调整。
- 在 Dashboard 区分当天计划、当天到期和此前未完成任务。
- 防止重复点击或请求重试生成重复习惯日志。

### 出口条件

- 任务与习惯的主要用户流程均通过界面和 API 验收。
- 页面刷新及程序重启后状态保持一致。
- 归档记录不再出现在默认列表，但仍可通过明确参数查询。
- 快速重复操作不会生成重复每日记录或错误的任务状态。

## M6：健康、睡眠与日记闭环

### 工作范围

- 实现体重、睡眠起止、睡眠时长、睡眠质量、精力、情绪、身体状态、运动分钟和备注。
- 自动计算跨午夜睡眠时长，并允许用户显式修正。
- 实现 Markdown 日记编辑与 1–2 秒防抖自动保存。
- 对空值、边界值、非法日期和数值格式提供明确校验反馈。

### 出口条件

- 每日健康和日记均采用 upsert，不产生同日重复记录。
- 睡眠时长计算覆盖同日与跨午夜场景，手工值不会被意外覆盖。
- 日记保存状态准确；失败输入不会丢失，重试成功后可在重启后读取。
- 健康数据允许部分填写，不因无关字段为空而拒绝保存。

## M7：数据保护、导出与迁移

### 工作范围

- 使用 SQLite backup API 或等效一致性机制实现每日首次启动备份和手动备份。
- 实现可配置的备份保留数量，默认保留最近 30 份。
- 实现结构化数据 CSV 导出、日记 Markdown 导出及可选 ZIP 封装。
- 编写并验证备份恢复、运行时目录迁移和异常中断处理说明。

### 出口条件

- 备份可以打开并通过完整性检查；备份失败不会破坏主数据库。
- 导出目录和文件位于 `LIFE_OS_HOME`，CSV 中文可被常见 Windows 工具正确读取。
- 在正常关闭后复制整个 `LIFE_OS_HOME` 到另一位置，应用能加载原有数据和配置。
- 新设备路径与旧设备不同也不影响运行，数据库和配置中不存在阻断迁移的绝对路径。

## M8：系统验收与 v0.1 发布

### 工作范围

- 完成需求提纲中的端到端验收场景及 [[接口验收清单]] 全部必选项。
- 覆盖启动、关闭、并发请求、事务失败、磁盘不可写、备份失败和导出失败等关键异常。
- 完善 README、安装升级、关闭、迁移、备份、恢复、导出和已知限制说明。
- 清理无用依赖、调试输出、敏感日志及误导性的未实现入口。

### 出口条件

- 自动化测试全部通过，手工 MVP 流程、迁移演练和恢复演练均有记录。
- 默认只监听 `127.0.0.1`，日志不包含日记正文或健康详情。
- 新环境可依据 README 完成安装、启动、关闭和数据迁移。
- 无阻断性缺陷；其余已知问题已记录影响、规避方式和后续版本归属。

## 变更控制

开发过程中若新增功能影响数据库结构、公共 API 或 `LIFE_OS_HOME` 边界，应先同步更新需求提纲、数据库模型和接口验收清单，再进入实现。非阻断性的 v0.2 候选不得插入当前里程碑。
