---
title: "Life OS 前端设计与技术架构提纲"
type: project-frontend-spec
created: 2026-09-18T15:54:29+08:00
updated: 2026-09-22T11:53:25+08:00
status: draft
source:
  - "[[Life OS 前端界面设计规范（Codex 执行版）]]"
related:
  - "[[项目说明]]"
  - "[[个人生活管理系统 Life OS v0.1 工程需求提纲]]"
  - "[[M1前端架构核查记录]]"
  - "[[M4前端建设记录]]"
  - "[[桌面封装架构与改造工作量评估]]"
  - "[[部署与迁移指南]]"
  - "[[M4.5桌面封装建设记录]]"
  - "[[界面主题设计规范]]"
  - "[[M5任务与习惯闭环建设记录]]"
  - "[[M7数据保护与迁移建设记录]]"
  - "[[M6健康日记与财务闭环建设记录]]"
---

# Life OS 前端设计与技术架构提纲

## 文档定位与执行约束

本提纲把项目内的 [[Life OS 前端界面设计规范（Codex 执行版）]] 整理为两套互相配合的执行规则：第一部分决定界面应该呈现怎样的视觉与交互体验，第二部分决定 v0.1 如何在现有 Flask 工程中实现、测试和部署这些界面。它是 M4–M6 及 M4.5 桌面承载工作的执行入口；字段、业务规则、接口行为和最终验收仍分别以 [[个人生活管理系统 Life OS v0.1 工程需求提纲]]、[[接口验收清单]]和[[里程碑计划]]为准。

M4 已于 2026-09-19 按本提纲完成，当前实现状态与浏览器验收结果见 [[M4前端建设记录]]。2026-09-20 的增量需求在同一 M4 前端上增加七套可切换主题和统一 Logo，不改动业务数据与页面语义，具体视觉定义见 [[界面主题设计规范]]。M4.5 已在不改写页面和业务 API 的前提下增加桌面窗口与便携发行层，实际结果见 [[M4.5桌面封装建设记录]]。M5 已继续复用既有 Token、API Client、页面日期状态和状态反馈组件，完成任务与习惯独立管理页及日期详情快捷操作；2026-09-21 增量又加入可创建单选分类、按需打开的编辑窗口、更清晰的品牌尺寸，以及彼此独立的“回到今日”模块导航与“回到今天”日历定位，结果见 [[M5任务与习惯闭环建设记录]]。M6 继续沿用同一架构，完成生活节律、日记和财务独立管理页及日期联动，结果见 [[M6健康日记与财务闭环建设记录]]。

若原始视觉规范与现有工程基线冲突，以已经确认的 v0.1 工程要求为准。当前版本固定使用 HTML5、CSS3 和 Vanilla JavaScript，由 Flask 同源提供页面与静态资源；不引入 React、Vue、Angular、Tailwind、Vite 或 Node.js 构建链。

---

## 第一部分：视觉设计提纲

### 1.1 设计目标与适用边界

Life OS 的界面首先帮助用户理解“我此刻的生活状态”，然后才承载记录、回顾与行动。首页应呈现有主次的生活线索，而不是由等权 KPI 卡片拼成的企业仪表盘。

视觉方向定义为“现代东方极简”：第一眼是清晰、成熟的现代 Web 应用，长期使用时能感受到克制、清雅和生命力。东方感来自色彩、留白、比例、文字层级与信息节奏，不依赖仿古装饰。

视觉规则约束页面风格、信息层级、组件状态和响应式表现。静态 Demo 用于确认审美方向，不代表最终数据结构或业务实现。

设计判断按以下顺序取舍：

> 信息清晰 ＞ 使用舒适 ＞ 视觉克制 ＞ 现代感 ＞ 东方气质 ＞ 装饰效果

### 1.2 视觉语言

#### 色彩

页面以宣纸暖白为底、墨色为正文、青黛为主色、天青为辅助色；朱砂与赭金只承担少量提醒和强调。

```css
:root {
  --background: #f5f3ed;
  --surface: #fbfaf6;
  --surface-muted: #eceae3;
  --text-primary: #242927;
  --text-secondary: #66706c;
  --text-muted: #929995;
  --border: #dcdcd4;
  --border-strong: #c6cbc5;
  --primary: #3f625f;
  --primary-hover: #345552;
  --primary-light: #dce8e4;
  --cyan: #729b99;
  --jade: #5e8076;
  --vermillion: #b95549;
  --gold: #a58955;
  --module-status: #eef4f1;
  --module-focus: #fbf3e8;
  --module-rhythm: #eaf3f3;
  --module-habits: #edf4ee;
  --module-notes: #f3eff3;
}
```

暖白约占页面的三分之二，墨色与灰色承担正文和结构，青色系集中用于操作、选中状态与趋势，其他强调色总量保持在 5% 以内。不得将整页染成青色，也不使用蓝紫渐变、霓虹、发光或玻璃拟态。

不同模块可以使用低饱和、明度接近的独立底色，帮助视线识别信息边界。底色只区分语义区域，不承担状态含义；正文对比度不得因底色改变而降低。

#### 字体与层级

英文、数字、时间和关键指标优先使用 `Times New Roman`。中文正文、表格和操作文本使用现代中文无衬线字体；页面日期、大标题或日记标题可少量使用宋体类字体建立气质。字体栈必须提供中文回退，毛笔字体不进入 UI。

```css
--font-ui: "Times New Roman", "PingFang SC", "Microsoft YaHei", sans-serif;
--font-title: "Times New Roman", "Songti SC", "STSong", serif;
```

| 层级 | 建议字号 | 用途 |
| --- | ---: | --- |
| 页面标题 | 28–32px | 日期、页面主标题 |
| 区块标题 | 18–20px | 页面主要分区 |
| 条目标题 | 15–16px | 列表、局部标题 |
| 正文 | 14–15px | 阅读与操作文本 |
| 辅助信息 | 12–13px | 时间、标签、说明 |
| 核心数字 | 28–40px | 仅用于少数关键指标 |

字重、字色和留白优先于字号跳跃。正文保持舒适行距，不用大面积粗体制造层级。

#### 空间、边界与形状

界面采用中低信息密度，通过稳定间距、明确边界和分区底色建立结构。页面仍接近“数字书页与个人工作台”，但各主模块必须能够被快速扫视和区分。

- 按钮与输入框圆角为 6–8px，内容区块为 8–10px，弹窗不超过 12px。
- 每个首页主模块使用独立浅底色、清晰边框和标题标记；同一模块内部减少重复套卡片。
- 模块名称只使用中文标题，并通过字重、色块或短竖线形成视觉入口，不增加英文标签。
- 按钮允许使用轻微位移、偏移阴影或图标旋转增加趣味，反馈幅度应克制且不妨碍操作。
- 同一层级采用一致的间距节奏，建议以 4px 为基础单位。
- 不使用巨大 Hero、夸张圆角、复杂背景纹理、大面积 Emoji 或彩色图标底座。

### 1.3 整体布局

桌面端采用克制的左侧导航与单一主内容区。导航只显示品牌、核心模块和少量辅助入口；图标与中文名称并列，当前项使用青黛浅底或左侧标识，不使用复杂装饰。

主内容区使用浏览器可用视口宽度，始终铺满侧栏之外的区域，不设置会在右侧留下大块空白的全局最大宽度。普通桌面保持清晰的纵向节奏；超宽屏把日详情模块自动组织为双列，并让状态总览与成对模块跨列，以控制单项内容的阅读长度。移动端将侧栏转换为顶部品牌栏与横向导航，内容保持单列，操作区域允许换行，但不压缩正文至难以阅读。

### 1.4 首页信息结构

首页围绕四个问题排序：今天状态如何、什么需要关注、近期发生了什么变化、下一步可以做什么。

1. 日期、问候与单一主操作。
2. 今日状态：一句概括性判断，加少量关键指标。
3. 今日重要事项：任务、日程或需要回顾的内容，突出优先级而非数量。
4. 生活节律概览：只保留睡眠时长、体重、运动和身体健康四项；睡眠时段、质量、精力、情绪和备注等详细信息在独立生活节律板块中展开。
5. 习惯与最近记录：支持快速确认和继续书写。

首页首屏不展示十几个等尺寸指标。关键结论应先于图表，操作应贴近对应内容。

### 1.5 组件表现

#### 导航与按钮

主按钮使用青黛实色，次按钮使用浅色背景和明确边框，危险操作使用朱砂但不扩大面积。按钮可通过 1–2px 偏移阴影、简洁图形和短促位移增加可操作感。导航、按钮和输入框必须有明确的悬停、按下、禁用与键盘焦点状态。

#### 状态与标签

状态文字优先于纯颜色表达。标签只用于必要的分类、提醒和完成状态，采用低饱和浅底，不为每条信息附加徽章。

#### 图表

图表与页面共用青黛、天青、竹青、赭金、藕灰和朱砂色板。财务界面另设跨主题稳定的资产、负债、收入、支出、转账和中性期间信息语义 Token，并同时使用文字、边线和金额颜色表达，不能只靠颜色区分。弱化网格线、坐标轴和图例，突出趋势与异常；不使用 3D、默认彩虹色板或装饰性渐变。数值必须配有时间范围和含义说明。

#### 图标

图标采用统一的线性风格和描边粗细，只辅助识别，不代替关键文字。没有图标也能读懂的内容，不强制增加图标。

### 1.6 交互、响应式与可访问性

交互反馈应轻、快、明确，颜色和边框过渡控制在 150–250ms。避免弹跳、飞入、频繁缩放和长时间动画；在 `prefers-reduced-motion` 下关闭非必要过渡。

页面需满足以下基础要求：

- 键盘可访问，焦点状态清晰可见。
- 正文与背景具有足够对比度，状态不只依赖颜色。
- 窄屏下保持可读顺序、合理点击区域和无横向溢出。
- 宽屏下主内容随视口扩展，超宽屏通过模块网格利用空间，不在右侧形成无意义背景区。
- 动态内容提供可理解的文字反馈。
- 七套主题均为浅色长期使用界面；主题切换不得改变语义顺序、数据状态或键盘路径。

### 1.7 可切换主题与品牌

共享导航提供“默认、古风竹青、古风藏青、古风水色、新拟物派、macOS 毛玻璃、吉卜力风格”七个选项。七套主题必须共用同一 HTML、响应式布局、组件状态和 SVG 图标语义，只通过 `data-theme`、CSS Custom Properties 与局部组件覆盖改变表现。主题选择使用 `life-os.theme`：当前同源页面写入 LocalStorage，并同步写入同一 WebView 配置中的本地 Cookie，以适配桌面服务重启后随机端口变化；两者均由 pywebview 限制在 `LIFE_OS_HOME/cache/webview/`。主题脚本不导入 API Client，不发送 `fetch`，无效值回退默认。

每套风格的色板、字体、形状、背景、阴影和图标规则以 [[界面主题设计规范]] 为唯一执行入口。默认主题延续现代东方极简；三套古风主题分别表达竹青纸本、藏青册页和水色雾面；新拟物派使用同色基面与内外阴影；macOS 毛玻璃使用半透明和背景模糊；吉卜力风格使用温暖自然色与纯 CSS 天空/云团，不复制具体角色或影视素材。

项目 Logo 固定使用 `design-assets/logo-concepts/life-os-logo-02a-ref-a-full-landscape-contained.png`。页面从 `static/images/life-os-logo.png` 加载同内容副本，PyInstaller 图标脚本从原始文件生成 ICO；不得继续使用旧的 CSS 圆环占位 Logo。侧栏标志与 ICO 内容区应接近 Chrome、ChatGPT 等常用应用的视觉占比。生成脚本必须按可见 Alpha 阈值排除源图边缘近乎透明的杂散像素，再将主体放大到画布安全边距内，不能只按非零 Alpha 外框裁切。

### 1.8 视觉验收标准

视觉 Demo 和正式页面都按以下标准检查：

- 各主题的主色、背景和层级是否符合其独立设计定义，同时保持舒适、可辨识的长期使用氛围。
- 左侧导航、信息主次和中低密度布局是否符合“生活状态首页”的定位。
- 各模块是否能通过标题、边界和独立浅底色被快速区分，同时保持整体统一。
- `Times New Roman` 的英文与数字是否和中文字体自然衔接。
- 按钮是否更具操作趣味，但没有产生浮夸或模板化观感。
- 日期标题、线性图标、趋势图和快速记录弹窗是否属于同一视觉语言。
- 桌面与窄屏的基本阅读体验是否成立。
- 主题切换是否即时、可恢复且不产生 API 请求；七套主题中的焦点、选中、错误与日期状态是否都清晰。

Demo 使用固定示例内容和浏览器内临时交互，不接入接口、不保存业务数据，也不代表正式组件或工程实现。

---

## 第二部分：前端技术架构提纲

### 2.1 架构目标与技术基线

v0.1 前端以“无需 JavaScript 构建即可随 Flask 本地运行、职责清楚、能够逐里程碑扩展”为目标。正式桌面运行链路固定为：

```text
pywebview 桌面窗口（WebView2）
  → 页面控制器与视图组件
  → 统一 API Client（fetch）
  → Waitress 承载的 Flask 页面与 /api 路由
  → 领域服务
  → SQLAlchemy / SQLite
```

前端不得直接读取 SQLite、Markdown、运行时目录文件或后端 Python 对象，也不得在 LocalStorage、Cookie、IndexedDB 或内存中保存业务数据的唯一副本。WebView 存储只允许保存可丢弃的界面偏好，例如主题选择；主题缺失、无效或存储失败时必须回退默认，不能阻断业务页面。

pywebview 是现有 Web 前端的桌面宿主，不是第二套业务客户端。桌面层只负责创建窗口、等待本地服务就绪、控制生命周期和显示启动错误；不开放新的 JS-Python 业务桥接，所有业务读写继续经过统一 REST API。外部浏览器运行链保留用于开发和诊断，不作为正式发行入口。

技术基线如下：

- 页面：Flask/Jinja 只提供页面骨架、静态资源地址和必要的非敏感启动配置。
- 样式：原生 CSS、CSS Custom Properties、响应式 Media Queries。
- 逻辑：现代 Vanilla JavaScript 与原生 ES Modules。
- 通信：原生 `fetch`，所有业务请求使用 `/api` 前缀和同源访问。
- 桌面宿主：pywebview，Windows 使用 WebView2；其缓存和持久化存储显式指向 `LIFE_OS_HOME/cache/webview/`。
- 服务承载：正式桌面模式使用 Waitress 监听随机可用的 `127.0.0.1` 端口；Flask 开发服务器和固定端口只用于源码调试。
- 发行形式：PyInstaller `onedir`，确保模板、静态资源、图标和运行依赖随包离线分发；目标设备无需 Python 或 Node.js。
- 图标：参考 `design-demo` 的统一线性描边语言，使用项目内联 SVG 或本地 SVG 文件；导航、按钮不得使用 Unicode 符号充当正式图标，不依赖在线图标服务。
- 主题：`theme.js` 只负责白名单校验、`data-theme` 切换、`theme-color` 更新与 WebView 本地偏好；LocalStorage 服务当前端口，Cookie 仅承担跨随机端口重启回退，均不进入业务 API；`themes.css` 只覆盖 Token 和装饰，不复制页面布局或业务选择器。
- 品牌：页面 Logo 和桌面图标必须来自已确认的 `life-os-logo-02a-ref-a-full-landscape-contained.png`，并随发行包离线提供。
- 第三方库：只有原生能力明显不足时才引入；必须固定版本、保存到 `static/vendor/` 并离线分发，不使用 CDN。

M1 环境已完成与本基线的实施前核查：现有 Python/Flask 依赖、Jinja 模板、同源静态资源、无 Node.js 启动链和 `LIFE_OS_HOME` 边界均兼容。应用骨架已提供版本化静态资源 URL 与 HTML 重新验证策略，详见 [[M1前端架构核查记录]]。

### 2.2 目录与模块划分

正式实现应在现有 `life_os/` 包内按以下结构演进；M4 初期允许合并过小文件，但不得把 API、状态和 DOM 操作重新混在单个脚本中。

```text
desktop_entry.py                    # 正式桌面入口与生命周期
LifeOS.spec                         # PyInstaller onedir 构建规则
requirements-build.txt              # 构建期依赖
build_desktop.bat                   # 可重复的 Windows 构建入口
life_os/
├── templates/
│   ├── base.html                 # 全局页面骨架与静态资源入口
│   ├── index.html                # 只呈现本地今天
│   ├── calendar.html             # 月历、任意日期详情、未来规划与日期标记
│   └── partials/
│       ├── sidebar.html          # 共享导航与线性 SVG 图标
│       └── day-dashboard.html    # Today 与 Calendar 共用的完整日期详情
└── static/
    ├── css/
    │   ├── tokens.css            # 色彩、字体、间距、圆角、层级
    │   ├── base.css              # Reset、排版、焦点与通用元素
    │   ├── layout.css            # 导航、页面网格、响应式结构
    │   ├── components.css        # 按钮、表单、状态、弹窗等
    │   ├── themes.css            # 七套主题 Token 和组件装饰覆盖
    │   └── pages/
    │       ├── today.css         # Today Dashboard 特有布局
    │       └── calendar.css      # 月历、完整日期详情与未来规划布局
    ├── js/
    │   ├── app.js                # 启动、导航和全局错误边界
    │   ├── theme.js              # 主题白名单、切换与可丢弃偏好
    │   ├── api/
    │   │   └── client.js         # fetch、JSON 包装、超时与错误转换
    │   ├── components/           # 无业务或轻业务的 DOM 组件
    │   ├── pages/
    │   │   ├── today.js          # 本地今天装配
    │   │   └── calendar.js       # 任意日期状态、未来规划与页面协调
    │   ├── features/             # calendar、tasks、habits、health、journal、finance
    │   └── utils/                # 日期、格式化、DOM 与校验工具
    ├── icons/                    # 本地 SVG
    ├── images/                   # 统一 Logo 等本地品牌图像
    └── vendor/                   # 经批准并固定版本的第三方资源
```

模块边界必须满足：`api/` 不操作 DOM，`components/` 不自行请求数据，`pages/` 负责装配而不复制业务规则，`features/` 只处理所属领域的界面状态和事件。CSS 类负责样式，JavaScript 事件优先使用 `data-*` 属性定位，避免脚本依赖纯视觉类名。

### 2.3 页面启动与日期导航

`app.js` 在 `DOMContentLoaded` 后根据页面标识启动 Today、Calendar、Tasks 或 Habits 控制器。Today 固定请求设备本地今天，不接受历史日期状态；Calendar 解析 URL 中的 `?date=YYYY-MM-DD`，并请求对应的月份与 `GET /api/day/{date}`；Tasks 与 Habits 分别维护所属领域的编辑、筛选与操作中状态。日期页面的基本流程为：

1. 渲染稳定的页面骨架和加载状态。
2. Today 发起当天聚合请求；Calendar 切换日期时取消仍未完成的旧请求。
3. 校验统一 JSON 包装，成功后一次性分发给各模块渲染。
4. 无数据时显示可行动的空状态；失败时显示错误和重试入口。
5. 只有最后一次日期选择的响应可以更新页面，避免快速切换造成旧数据覆盖。
6. Calendar 选择日期只更新完整日期详情、标记表单、未来规划状态与 URL，不整页刷新。页首“回到今日”是指向 `/` 的普通模块链接；月历工具栏中的“回到今天”才读取设备本地日期，并等待目标月份加载与日期详情请求共同完成后再恢复按钮，避免远月份切换时出现旧月历覆盖新选中态。月历日期格用更大的公历数字作为主视觉，并通过 `Intl.DateTimeFormat("zh-CN-u-ca-chinese")` 在本机离线派生农历短标签；不引入在线农历接口或新的持久化字段。

今日页的任务、习惯、健康、日记和当日财务只从本地今天派生。日历页允许访问过去、今天和未来；所选日期复用 Today 的完整详情组件，任务、习惯、健康与睡眠、日记、财务和日期标记都从同一个页面日期状态派生。未来日期额外显示快速规划表单，通过 `POST /api/tasks` 创建 `scheduled_date` 为所选日期的任务；完整编辑在 `/tasks` 中完成。日期详情允许即时完成或重新打开任务，并允许在今天或过去日期完成或撤销习惯；未来日期不写入习惯日志。日期在前端和 API 间统一使用 `YYYY-MM-DD` 字符串，避免先转换为 UTC 导致跨日。财务总体摘要是独立读取模型，不将全量历史流水塞入每日聚合。

任务与习惯管理页默认隐藏新增/编辑表单，仅保留清单和触发按钮。点击“新建任务”或“新建习惯”后打开原生 HTML `<dialog>` 页面内模态窗口；编辑复用同一窗口。保存成功后关闭，取消、关闭按钮与 Escape 不写入，关闭后焦点回到触发按钮。这里的“独立窗口”是应用内部编辑窗口，不创建第二个 pywebview 原生窗口。

任务、习惯与财务分类使用共用 `CategorySelect` 组件和原生单选 `<select>`。组件从 `/api/categories?scope=...` 读取选项，在当前表单内展开“新建分类”，创建成功后立即加入列表并自动选中；三个作用域相互分离。任务与习惯不允许自由文本直接写入业务记录，日历未来计划复用任务分类选项；财务接口保留读取旧自由文本分类的兼容性，页面新写入只从选项中选择。

### 2.4 API Client 与错误模型

所有请求必须经过单一 `api/client.js`，页面和组件不得散落裸 `fetch`。Client 至少负责：

- 设置 `Accept: application/json`，有请求体时设置 `Content-Type: application/json`。
- 解析 `{"success": true, "data": ...}` 与 `{"success": false, "error": ...}`。
- 区分参数错误、未找到、冲突、服务异常、连接失败和请求取消。
- 从响应头保留 `X-Request-ID`，在错误详情中提供给用户或日志排查。
- 通过 `AbortController` 支持日期切换取消和合理超时。
- 对空响应、非 JSON 响应和非预期字段给出受控错误，不让解析异常直接破坏页面。

错误提示使用用户可理解的文字，不显示 Python traceback、绝对路径或内部对象。前端不擅自重试写请求；读取请求可在用户触发下重试。

### 2.5 页面状态与保存策略

每个数据区域都必须显式支持 `loading`、`ready`、`empty`、`error` 五种读取状态；可编辑区域另支持 `dirty`、`saving`、`saved`、`save-failed`。状态显示必须有文字，不只依赖颜色或动画。

- 习惯切换与任务状态变更立即提交。提交期间禁用重复触发；失败后恢复可编辑状态并提供重试。
- 健康字段在失焦或短暂防抖后保存。新输入到达时取消尚未发出的旧防抖任务，但不得隐式丢弃已经失败的值。
- 日记采用明确的防抖自动保存，并显示最后成功保存时间；离开页面前若仍有未完成保存，应提示用户。工具栏支持插入设备当前 `HH:mm`、一级至三级标题和本地图片，编辑器旁提供安全实时预览与单日 Markdown 导出。
- 财务流水不自动保存；只在用户明确提交且金额、类型与账户方向校验通过后写入。提交中禁用重复操作，失败时保留表单值。
- 使用乐观更新时必须保留旧值，失败后回滚或清楚标记“尚未保存”，不能把临时界面状态当成成功结果。
- 页面级状态保存在对应控制器内；v0.1 不引入 Redux、Zustand 或自建全局状态框架。

### 2.6 组件与 DOM 规则

组件采用小型渲染函数、`<template>` 元素或局部控制器实现，输入为明确数据，输出限定在自身容器。组件不得读取未声明的全局变量，也不得跨模块查询和修改任意 DOM。

- 所有用户文本默认通过 `textContent` 写入；禁止把 API 内容直接传给 `innerHTML`。
- Markdown 日记预览使用仓库内固定实现，只通过 DOM 文本节点生成标题、段落、列表、引用、粗体、行内代码和本地图片，不使用 `innerHTML`，也不加载外部解析器或远程图片。
- 表单控件必须有 `<label>`，动态状态使用适当的 `aria-live`，弹窗关闭后恢复触发按钮焦点。
- 列表项使用后端稳定 ID 作为 `data-id`，不以数组位置或展示文本作为业务标识。
- 事件监听在组件初始化时集中注册；重复渲染不得重复绑定。

### 2.7 CSS 与视觉落地规则

正式 CSS 以 `tokens.css` 定义默认核心视觉变量，以 `themes.css` 按受控主题覆盖变量和少量组件装饰。颜色、字体、间距、圆角、边框、阴影、层级和图表色板均使用变量，不在页面脚本或业务组件中散落主题判断或十六进制颜色。

样式按 Token → 基础排版 → 布局 → 组件 → 页面 → 主题覆盖的顺序加载。组件使用可读的语义类名，状态使用 `.is-loading`、`.is-error` 或 `data-state`；不得依赖深层选择器和 `!important` 维持正常样式。主题选择器只设置 `<html data-theme>`，不得给每个组件逐一写行内样式。桌面优先实现，但每套主题都必须在 1280px、768px 和 375px 三个代表宽度检查无溢出、无内容遮挡、焦点可见。

### 2.8 依赖、安全与离线要求

v0.1 默认不引入 JavaScript 前端运行时依赖。pywebview 与 Waitress 属于 Python 桌面宿主和服务运行依赖，不改变前端页面的 Vanilla JavaScript 基线。确需图表、月历或 Markdown 库时，先记录用途、许可证、版本、文件哈希与替代方案，再将压缩文件纳入 `static/vendor/`。普通运行不能访问 CDN、远程字体、分析服务或跟踪服务。

前端只连接 `127.0.0.1` 上由当前应用提供的同源 API，不添加跨域配置。桌面模式使用进程内协商的动态端口，不将其视为稳定公开接口。所有外部输入按不可信内容处理，不执行内联脚本，不动态拼接事件属性。敏感诊断信息只写入后端日志；WebView 或浏览器控制台不得输出完整健康记录、日记正文或运行时绝对路径。

### 2.9 测试与里程碑实施顺序

前端开发按现有里程碑推进，不一次性铺开全部模块：

1. M4：页面骨架、Token、Today Dashboard、独立 Calendar 任意日期访问与未来快速规划、月历导航、API Client、读写状态组件、七套主题、统一 Logo 和错误恢复。
2. M4.5：使用 Waitress + pywebview 承载现有页面，建立窗口/服务生命周期、WebView 主题偏好存储路径、Logo 图标生成和 PyInstaller `onedir` 发行基线。
3. M5：任务与习惯的完整界面闭环，包括重复点击保护和归档后的默认隐藏。已完成。
4. M6：健康、睡眠、日记和财务，包括字段解释、防抖保存、跨日睡眠展示、未保存保护、明确提交记账和总体财务摘要。
5. M7：在 Settings 中接入备份、导出、系统信息及恢复反馈。已完成：运行时目录可复制，备份数量可配置，自动/手动备份清单可见，全量导出显示目录或 ZIP 路径；恢复必须选择已校验备份并输入确认短语，安排后明确提示正常关闭和重新启动，不在页面请求期间改写活动数据库。

每阶段至少覆盖：Python 端页面/静态资源与 API 集成测试、浏览器手工主流程、键盘流程、窄屏流程、加载/空/错状态，以及刷新和程序重启后的数据一致性。主题增量还需验证七个白名单值、无 API 调用、偏好恢复、清缓存回退、Logo 完整性和代表宽度；新增第三方脚本时补充离线启动验证和许可证记录。

### 2.10 正式部署与发布检查

v0.1 没有 JavaScript 前端构建步骤。HTML、CSS、JavaScript、SVG 和获批的 vendor 文件由 Flask 的 `templates` 与 `static` 目录提供，并由 PyInstaller 收集到 `onedir` 桌面发布包中；页面和 API 同源，因此不需要 CORS、反向代理或单独前端服务。正式运行由 Waitress 承载 Flask，pywebview 仅显示本地页面；`start.bat` 与外部浏览器用于开发诊断。

发布前按以下顺序执行：

1. 确认所有静态资源使用本地相对路径，不存在 CDN、远程字体或运行时下载。
2. 运行完整 `pytest`，确认页面入口、静态资源和既有 API 测试通过。
3. 构建 PyInstaller `onedir` 包，核对模板、静态资源、图标、许可证、版本信息和运行依赖均已包含。
4. 在未安装 Python 的干净 Windows 环境双击 `LifeOS.exe`，确认只出现桌面窗口，不出现终端和外部浏览器；关闭窗口后服务、端口和锁均释放。
5. 在 1280px、768px、375px 代表宽度完成视觉与键盘检查，并验证系统缩放下窗口可用。
6. 清空 `LIFE_OS_HOME/cache/webview/` 后再次启动，确认资源版本更新且没有旧脚本与新 HTML 混用。
7. 使用自定义 `LIFE_OS_HOME` 做一次烟雾和迁移测试，确认前端资源来自程序目录，业务数据与 WebView 存储均只写入运行时目录。
8. 使用 `start.bat` 验证源码诊断路径，确保与桌面模式共用同一页面、API 和领域实现。
9. 确认发布包不包含开发日志、用户数据、临时数据、构建缓存、源映射中的敏感路径或未批准依赖。
10. 依次切换七套主题并重启桌面窗口，确认偏好恢复、Logo 与 EXE 图标一致、业务数据无变化；清空 `cache/webview/` 后确认安全回退默认。

HTML 入口应使用不缓存或重新验证策略；静态资源采用应用版本或内容版本参数实现缓存失效。资源版本由 Flask 模板统一注入，不在多个页面手工维护。

### 2.11 前端完成定义

一个前端功能只有同时满足以下条件才可关闭：

- 业务流程通过界面与对应 API 验收，刷新后与数据库状态一致。
- 加载、空、成功、错误、保存中和保存失败状态均可复现并有明确反馈。
- 用户输入在请求失败时不丢失，重复操作不会产生重复数据。
- 键盘、窄屏和减少动画偏好下不受阻断。
- 视觉表现符合第一部分 Token、模块边界、字体和组件状态规范。
- 七套主题共用同一语义 DOM 与业务逻辑，选择偏好可恢复，清缓存可回退，Logo 来源符合品牌约束。
- 无新增远程依赖、未说明的全局状态或绕过 API 的数据访问。
- 在源码浏览器模式和冻结桌面模式中行为一致，WebView 存储不越过 `LIFE_OS_HOME`，关闭窗口后不残留后台服务。
- 测试、部署说明和变更记录已同步更新。

## 本次整理说明

原规范中的视觉目标、首页原则、布局、字体、配色、图表与交互要求已合并去重，并根据本轮反馈补充 Times New Roman、模块底色、清晰边界与更有趣的按钮反馈。原文中的 React、Vite、Tailwind、TanStack Query 和 FastAPI 假设与已确认的 v0.1 工程基线冲突，已替换为可直接落入当前 Flask 工程的 Vanilla JavaScript 架构。根据最新范围变更，当日记账与个人总体财务摘要已纳入 v0.1，前端实施按 M4 预留容器、M6 完成交互闭环的顺序推进。2026-09-20 新增的主题需求通过单一 DOM、`theme.js` 和 `themes.css` 实现，七套具体视觉定义集中在 [[界面主题设计规范]]，不引用早期尝试版 HTML 作为实现来源。
