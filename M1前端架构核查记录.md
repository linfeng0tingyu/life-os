---
title: "Life OS M1 前端架构核查记录"
type: engineering-audit
created: 2026-09-18T16:34:26+08:00
updated: 2026-09-20T14:51:41+08:00
status: draft
related:
  - "[[前端设计与技术架构提纲]]"
  - "[[里程碑计划]]"
  - "[[部署与迁移指南]]"
  - "[[桌面封装架构与改造工作量评估]]"
---

# Life OS M1 前端架构核查记录

## 核查目标

本记录核对 M1 工程与运行环境是否能直接承载 [[前端设计与技术架构提纲]] 中的 Flask/Jinja、Vanilla JavaScript ES Modules、原生 CSS、同源 `/api` 与离线静态资源方案。核查不提前实现 M4 页面功能，只消除会阻断后续实施的环境与入口问题。

## 环境与依赖结论

| 核查项 | 当前状态 | 结论 |
|---|---|---|
| Python | 要求 3.12+，当前验证环境为 3.14.6 | 满足 |
| 后端依赖 | Flask 3.1.x、Flask-SQLAlchemy 3.1.x、SQLAlchemy 2.0.x | 满足 |
| 前端构建 | 无 Node.js、npm、Vite 或前端编译步骤 | 与提纲一致 |
| 模板与静态资源 | Flask `templates/` 和 `static/` 同源提供 | 满足 |
| 浏览器 | 现代 Edge、Chrome 或 Firefox | 支持 ES Modules、fetch、AbortController 和 CSS 变量 |
| 网络边界 | 只监听 `127.0.0.1`，页面与 API 同源 | 无需 CORS 或独立前端服务 |
| 离线运行 | 普通运行无 CDN、远程字体或第三方请求 | 满足 |

`requirements.txt`、`pyproject.toml` 与 `start.bat` 不需增加前端运行时依赖。M4 可直接在 `static/css/`、`static/js/`、`static/icons/` 与 `static/vendor/` 中按提纲拆分资源；只有确认引入第三方前端库时才需记录版本、许可证和哈希。

## 本次补齐的实施前提

- 新增 `templates/base.html`，统一提供 UTF-8、viewport、应用版本元数据和静态资源入口；`index.html` 改为继承该骨架。
- 静态资源 URL 由 Jinja 注入应用版本查询参数，避免后续 HTML 与旧 CSS/JavaScript 缓存混用。
- HTML 响应设置 `Cache-Control: no-cache`，静态文件开发阶段不使用长时强缓存。
- 现有 API 包装、`X-Request-ID`、同源路径和错误隐私边界可直接供 M4 统一 API Client 使用。
- 业务数据、配置、日志和应用缓存仍只写入 `LIFE_OS_HOME`；随程序发布的 HTML/CSS/JavaScript/SVG 保持在程序目录。

## 自动化核查

前端环境冒烟测试验证根路径返回 HTML 200、版本元数据正确、CSS 路径带应用版本、HTML 使用 `no-cache`，且页面骨架不包含远程资源。全量 pytest 同时验证应用在自定义临时 `LIFE_OS_HOME` 中初始化，不将业务状态写入源码目录。

## 结论与边界

M1 环境可正常实施当前前端提纲，无需引入 Node.js、前端构建器、CDN 或新的 Python 运行时依赖。M4 仍需按提纲创建 CSS Token、ES Module API Client、页面状态机和响应式布局；这些属于前端功能实现，不是 M1 环境缺口。

## 2026-09-20 桌面封装增量核查

原核查结论仍适用于 M4 页面工程，但不足以直接作为普通用户桌面发行基线。新增的 pywebview + PyInstaller 决策不会推翻 M1 的 Flask app factory、同源页面、统一 `LIFE_OS_HOME`、实例锁、日志和健康检查；这些可以原样复用。需要在独立的 M4.5 中补齐以下增量，而不是回写或重做 M1：

| 增量项 | M1 现状 | M4.5 要求 |
|---|---|---|
| 服务宿主 | Flask 开发服务器 | 正式模式改为 Waitress，开发服务器仅保留诊断用途 |
| 用户入口 | `start.bat`、终端和外部浏览器 | `LifeOS.exe` 与 pywebview 独立窗口 |
| 端口 | 配置中的固定 `127.0.0.1:5000` | 正式模式使用预绑定的随机回环端口 |
| 运行依赖 | 目标机创建 `.venv` 并安装 Python 包 | PyInstaller `onedir` 内置 Python 与依赖，目标机只需 WebView2 Runtime |
| 资源路径 | 从源码目录读取模板和静态文件 | 同时支持源码路径与 PyInstaller 冻结资源路径 |
| 界面缓存 | 外部浏览器自行管理 | pywebview/WebView2 存储强制进入 `LIFE_OS_HOME/cache/webview/` |
| 生命周期 | 终端进程决定服务存活 | 关闭窗口时协调停止 Waitress、checkpoint、端口和实例锁 |

因此，M1 的环境和基础模块可以继续承载最新前端设计；桌面发行能力是可隔离的宿主层增量，不需要 C#、Node.js 或第二套前端。上述增量已在 M4.5 完成：冻结路径、WebView 缓存目录、桌面依赖引导和应用生命周期均已落地，源码诊断链保持兼容。具体实现与验证见 [[M4.5桌面封装建设记录]]，原工作量估算见 [[桌面封装架构与改造工作量评估]]。
