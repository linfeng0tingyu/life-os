---
title: "Life OS M4 前端建设记录"
type: engineering-delivery
created: 2026-09-19T21:31:25+08:00
updated: 2026-09-19T21:31:25+08:00
status: draft
related:
  - "[[项目说明]]"
  - "[[里程碑计划]]"
  - "[[前端设计与技术架构提纲]]"
  - "[[接口验收清单]]"
---

# Life OS M4 前端建设记录

## 建设范围

M4 在既有 Flask/Jinja 与同源 API 基线上完成基础界面和日期导航，不引入 Node.js、前端构建器、CDN 或第三方运行时依赖。实现包含桌面优先的侧栏与主内容布局、Today Dashboard、月历、日期属性编辑、统一 API Client、页面级日期状态以及加载、空、错误、保存中、已保存和保存失败反馈。

任务与习惯在本阶段读取并展示日期聚合结果，完整编辑闭环留给 M5。健康、日记与财务在本阶段展示只读摘要或稳定容器，完整保存与统计界面留给 M6。这一边界避免在 M4 重复实现后续领域交互，同时保证首页信息结构和页面模块位置已经稳定。

## 工程结构

样式按 `tokens.css`、`base.css`、`layout.css`、`components.css` 和 `pages/today.css` 顺序加载，核心颜色、字体、间距、圆角与阴影只在 Token 层定义。页面采用现代东方极简视觉方向，在桌面端使用固定侧栏和双栏 Dashboard，在中窄屏转为顶部导航与单列内容。

JavaScript 使用原生 ES Modules。`api/client.js` 统一处理同源请求、JSON 合同、超时、请求取消、连接失败、HTTP 错误和 `X-Request-ID`；`pages/today.js` 管理选中日期、URL、旧请求取消与最后响应生效规则；`features/calendar.js` 管理月历、日期选择和日期标记表单；组件只负责限定容器内的安全 DOM 渲染，所有 API 用户文本均通过 `textContent` 写入。

## 接口补充

`GET /api/calendar/month/{year-month}` 在原日期分类字段外新增两个轻量字段：`has_data` 表示该日存在显式日历标记或任务、习惯日志、健康、日记、财务流水；`habit_summary` 返回当前启用习惯的完成数与总数。该设计让月历一次请求即可显示数据提示和最小习惯摘要，避免每月发起约三十次完整日期聚合请求。

## 状态与错误处理

切换日期会取消尚未完成的旧请求，并以请求序号阻止迟到响应覆盖新日期。读取错误在页面顶部展示用户可理解的信息和重试按钮；若后端返回请求编号，错误信息会保留该编号用于排查。日期标记只有明确提交后写入；保存失败时不清空输入，并恢复表单可编辑状态。页面业务数据不写入 LocalStorage 或 IndexedDB。

## 验证结果

- 86 项 pytest 测试全部通过，包括月历轻量摘要、HTML 入口和全部 M4 本地静态资源。
- 浏览器默认打开本地今天；选择日期只更新 URL 与必要区域，不整页刷新。
- 日期标记保存后同步更新 Dashboard 与月历，刷新页面后仍从 SQLite 正确恢复。
- 上月、下月与返回今天行为正确；键盘 Tab 可进入主导航，表单具备可读标签和动态文字状态。
- 人工停止本地服务后，页面显示连接失败和重试入口；服务恢复后点击重试即可重新进入已载入状态。
- 1280px、768px 和 375px 视口均无横向溢出；桌面使用双栏，较窄视口使用单栏并将侧栏转为顶部导航。
- 浏览器控制台无错误；页面运行未请求 CDN、远程字体、分析服务或其他外部资源。

## 后续边界

M5 应在现有任务与习惯容器中增加创建、编辑、排序、状态切换、归档和失败回滚，不改变日期状态与 API Client。M6 应在现有节律、日记和财务容器中加入防抖保存、未保存保护、明确提交记账和总体财务摘要，并继续复用当前状态文案与错误模型。
